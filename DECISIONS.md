# Decisions

仕様書 0 節の指示に従い、仕様に書かれていない点は勝手に削らず、合理的な既定値を採用してここに記録します。各項目は「決めたこと / 理由 / 見送った案 / 見直す条件」で書きます。

---

## D-001 API は FastAPI（Python）

**決定** 仕様書 22 節が挙げる FastAPI と NestJS のうち FastAPI を採用。

**理由** 後続フェーズの重い部分がすべて Python 側にあります。Phase 5 の数式検証は SymPy / 次元解析、Phase 3 の埋め込みとクラスタリングは numpy / scikit-learn、Phase 5 の LaTeX 解析も Python のエコシステムが厚い領域です。API を TypeScript にすると、これらを別サービスに切り出して言語間の境界を 1 つ増やすことになります。

**見送った案** NestJS。モバイルと言語が揃う利点はありますが、上記の処理系を worker として別に持つ必要があり、Phase 5 で必ず払うコストが増えます。

**見直す条件** 数式パイプラインを最初から独立サービスにすると決めたとき。

---

## D-002 語彙は 1 つの JSON を両言語が読む

**決定** `packages/shared-types/enums.json` を唯一の定義とし、Python は実行時に読み込み、TypeScript はリテラル型として再掲したうえでテストで一致を検証する。

**理由** 「保存理由」「検証状態」「provenance の種別」のような語彙は、クライアントとサーバとデータベースの 3 か所に現れます。3 か所で独立に書けば必ずずれ、しかも ずれた瞬間ではなく 数週間後に本番で気づくことになります。1 か所を正とし、ずれをテストで落とすほうが安いと判断しました。

**見送った案** OpenAPI からのコード生成。生成物のレビューとビルド順序の管理が必要で、Phase 0 の規模に対して重すぎます。API の型が増えたら再検討します。

**見直す条件** 語彙以外の型も共有したくなったとき（そのときは OpenAPI 生成に移行）。

---

## D-003 列挙型は VARCHAR + CHECK 制約（PostgreSQL ENUM を使わない）

**決定** 語彙列は `VARCHAR` にし、`enums.json` から生成した `CHECK` 制約を付ける。

**理由** ネイティブ ENUM に値を足すには `ALTER TYPE` が要り、削除や並べ替えはさらに面倒です。この仕様書の語彙は今後まず間違いなく増えます（保存理由、数式カード種別、推薦理由）。`CHECK` なら制約の張り替えだけで済み、しかも定義を共有ファイルから生成できます。

**代償** DB 側の型安全性はやや弱くなります。これは `tests/test_vocab_parity.py` が制約と共有語彙の一致を検証することで補っています。

---

## D-004 スキーマは Phase 0 で全エンティティを作る

**決定** 仕様書 23 節の全 26 テーブルを最初のマイグレーションで作る。Phase 4–6 でしか使わない `equations`、`math_cards`、`canvas_positions` も含む。

**理由** あとから列を足すのは、既存行のバックフィルとデフォルト値の決定を伴います。空のテーブルが 10 個ある costs はほぼゼロで、リレーションの形を最初に一度考えておくほうが、フェーズごとに考え直すより設計が一貫します。

**代償** 使われていないテーブルを見た人が混乱する可能性。ARCHITECTURE.md の表でどのフェーズが使うかを明示しています。

---

## D-005 サンプルデータは合成テキスト

**決定** 実在論文の Abstract を同梱せず、分野ごとの文型バンクから決定論的に生成した 60 件を CC0 で置く。

**理由** 仕様書 31 節 4 項が明示的にこれを求めています。arXiv の Abstract は個々の投稿のライセンスに従い、一括で再配布可能とは限りません。公開リポジトリに置く以上、確認できないものは置かない、が唯一安全な選択です。

**品質のための工夫** 単なるプレースホルダーではなく、分野ごとに Background → Problem → Method → Result → Significance の 5 文で構成し、インライン LaTeX、DOI/arXiv 識別子、掲載誌、英語難易度、数式密度を持たせています。重複排除を試すための変種（DOI 表記ゆれ / arXiv v1・v2 / プレプリントと出版版）も 3 組含みます。

**Phase 1 以降** 実データは API から取得し、このコーパスはオフライン動作とテストのフォールバックとして残します。

---

## D-006 pgvector 列は Phase 3 まで作らない

**決定** `embeddings.vector_json` に JSON として保存し、`vector(N)` 列と ANN インデックスは Phase 3 のマイグレーションで足す。ただし docker-compose と CI は最初から `pgvector/pgvector:pg16` を使う。

**理由** Phase 0 にベクトル検索は 1 つもありません。次元数は使う埋め込みモデルで決まり、まだ決めていない値をスキーマに焼き込むと、決めた瞬間にマイグレーションのやり直しになります。一方で、環境側に拡張が無いせいで Phase 3 が止まるのは避けたいので、コンテナイメージだけ先に揃えてあります。

**`model` と `version` を Phase 0 から一意キーに含めている理由** 再埋め込み時に、異なるベクトル空間の値が同じテーブルに混在すると、距離計算が静かに壊れます。あとから足せる制約ではないので最初から入れています。

---

## D-007 ゲスト認証のみ、トークンはメモリ内

**決定** Phase 0 は `POST /auth/guest` だけを実装し、HS256 の JWT を返す。モバイル側はメモリにのみ保持する。

**理由** 仕様書 4 節のオンボーディングはサインアップを要求しません。最初のカードに辿り着くまでにアカウント作成を挟まないことが体験の前提なので、ゲストが一級市民である必要があります。メール認証は、それを壊さない形であとから足せます。

**Phase 1 で変えること** トークンを `expo-secure-store` に移します。アクセサの形（`getToken()`）は変えないので、呼び出し側は影響を受けません。

**安全側の既定** `PAPERMATCH_ENVIRONMENT` が `development` / `test` 以外のとき、開発用の既定秘密鍵のままだとアプリは起動を拒否します。

---

## D-008 ライセンス不明のレコードは保存しない

**決定** `abstract_redistributable` が false のレコードは取り込み時にスキップし、件数を監査ログに記録する。

**理由** Abstract を削って保存する案も考えましたが、Abstract の無いカードは Discover で表示できず、行が残っていればいつか誰かが「せっかくあるから」と使ってしまいます。仕様書 21 節の趣旨は「不明なものは扱わない」なので、行ごと作らないほうが意図に忠実です。

**代償** 候補が減ります。fixture では 60 件中 11 件がこれに該当し、パイプラインがこの分岐を必ず通るようにしてあります。

**見直す条件** メタデータのみのカード（タイトル・著者・原文リンクだけを見せ、Abstract は出典サイトで読ませる）を導入すると決めたとき。

---

## D-009 数式の途中で切れた選択は Abstract 全体を見て判定する

**決定** `selection_splits_math(full_text, start, end)` は選択範囲ではなく Abstract 全体を受け取る。

**理由** 実装中に判明した点です。ユーザーがある数式の途中から次の数式の途中までドラッグすると、選択範囲は `"$ ... where $"` という形になり、**構文的には完全に正しいインライン数式**として mask されてしまいます。選択範囲だけを見ている限りこれは検出できません。全体を mask して境界が数式スパンの内側にあるかを見れば確実に判定できます。

**結果** `POST /translations` はこの場合 422 `selection_splits_formula` を返します。壊れた LaTeX を含む翻訳を出すよりも、選択し直してもらうほうが良いと判断しました。

---

## D-010 翻訳が数式を壊したら原文を返す

**決定** Provider の出力にプレースホルダーの欠落があれば、翻訳ではなく原文を返し、`fellBackToOriginal: true` を立てる。

**理由** 数式が 1 つ抜けた翻訳は、抜けたことがユーザーから見えません。「訳せませんでした」と分かるほうが、静かに間違った内容を読ませるより安全です。仕様書 3 節の Trust by provenance と 7 節の数式保護の当然の帰結です。

**例外** 段階ヒントの `hard_words` は難語の一覧であって文の再現ではないため、数式を含まないのが正常です。この段階だけ欠落検査を行いません。

---

## D-011 タイトル正規化は `&` を `and` に寄せる

**決定** `normalize_title` は NFKC 正規化・ダッシュ統一・LaTeX 除去に加えて、`&` を ` and ` に置換する。

**理由** テスト作成中に見つかった実際の分岐です。同じ論文でも、Crossref と OpenAlex で `"Anomaly Inflow & Defects"` / `"Anomaly Inflow and Defects"` のように割れることがあります。正規化しないと、DOI を持たないレコード同士が別物として残ります。

---

## D-012 UI 文字列は Phase 0 から i18n 化する

**決定** 日本語 UI しか出さない段階でも、可視文字列をコンポーネント内に直接書かない。

**理由** 仕様書 25 節が明示しています。あとから i18n 化すると、全ファイルを走査して文字列を探す作業になり、必ず取りこぼします。最初から辞書経由にしておけば追加コストはほぼゼロです。英語辞書も同時に用意し、キーの欠落はテストで検出します。

---

## D-013 TypeScript のテストランナーは用途別に分ける

**決定** `packages/*` は Node 組み込みの `node --test`、`apps/mobile` は `jest-expo`。

**理由** 共有パッケージは React にも React Native にも依存しない純粋な TypeScript なので、jest を持ち込む理由がありません。逆にモバイルは React Native のモックが要るため `jest-expo` が必要です。それぞれに合ったものを使い、ルートの `npm test` が両方を呼びます。

**注記** `jest-expo@57` は jest 29 系を前提としています（jest 30 を入れると `jest-runtime` と `jest-mock` のバージョンがずれて全スイートが起動時に落ちます）。`apps/mobile` の jest は 29 に固定しています。

---

## D-014 Expo の依存はバージョンを直接指定する

**決定** `npx expo install` ではなく `npm install` で SDK 57 対応バージョンを明示的に指定する。

**理由** この開発環境では `expo install` が Expo の互換性 API に到達できません（プロキシが 403 を返す）。SDK 57 に対応するバージョンを `package.json` に固定してあるので、インストールは `npm ci` だけで再現します。

**注意点** SDK を上げるときは、ネットワークが通る環境で `npx expo install --fix` を実行し、その結果を commit してください。

---

## D-015 `index.html` は残す

**決定** リポジトリに元からあった無関係な `index.html`（参考文献リンク集）を削除しない。

**理由** PaperMatch とは関係のないファイルですが、削除を依頼されていません。不要であれば削除は 1 コマンドです。

---

## D-016 実データ Provider（arXiv / OpenAlex）は Phase 1 で未接続

**決定** Phase 1-A（arXiv / OpenAlex Provider）を実装せず、Phase 1-B と 1-C を先に完成させた。

**理由** この開発環境のネットワークポリシーがパッケージレジストリ以外への外向き接続を遮断しています（`export.arxiv.org` と `api.openalex.org` はプロキシが 403 CONNECT を返します）。Provider を書くこと自体はできますが、実際のレスポンスに対して一度も動かせないコードを「完了」として commit するのは誠実ではありません。Atom XML と OpenAlex JSON の記録済みレスポンスに対するオフラインテストは可能なので、Phase 1-A はその形（記録レスポンス + 手動での疎通確認手順）で次のスライスとして実装します。

**現状の影響** フィードは合成コーパス 49 件で動作します。仕様書 29 節の「100件以上の実データ候補」は未達で、これは Phase 1-A の完了条件のままです。

**追記（Phase 1-A 実装後）** Provider 本体は記録形状の fixture に対して完成しました（D-025）。実 API への疎通だけが `-m live` の opt-in テストとして残っています。

なお、遮断そのものが 1 件バグを見つけました。プロキシが返す 403（HTML 本文）を、パーサが「unparseable XML」として報告していました。`HttpProviderClient` は 4xx をあえて raise せずに返す設計（こちら側のクエリ不備でブレーカーを開かないため）なので、**ステータス確認は Provider 側の責任**です。これを怠ると「arXiv がリクエストを拒否した」が「XML が壊れている」として報告され、次に調べる人がパーサを開くことになります。両 Provider に `_fetch` を追加してステータスを先に見るようにしました。

---

## D-017 フィードの 70/20/10 は「枠の配分」であり、スコアの重みではない

**決定** 候補を「選択分野 / 隣接分野 / 未探索」の 3 プールに分け、7:2:1 のブロックを繰り返して並べる。

**理由** 仕様書 16 節の 70/20/10 を score の重み係数として実装すると、比率はどこにも保証されません。枠として実装すれば比率が構造的に保たれ、しかも各カードの推薦理由が本当のことになります（隣接プールのカードは「隣接だから」そこにいる）。

**実装中に判明したこと** 多様性の並べ替えを別パスとして後段に置くと、この比率が崩れます。隣接カードを後ろに送ると選択分野のカードが前に繰り上がり、7/2/1 のはずのページが 9/1/0 になりました。そのため「枠がプールを決め、その中で多様性が候補を決める」1 パスに統合しています。

**もう 1 点** プール判定に分野の重みしきい値（0.15）が必要でした。閾値なしだと、重み 0.1 の付随タグだけで機械学習の論文が「高エネルギー理論の選択分野」に入り、比率が事実上崩壊します。

---

## D-018 範囲選択は文単位

**決定** 任意のドラッグではなく、文をタップして選択し、隣の文をタップすると範囲が伸びる方式。

**理由** React Native の `selectable` テキストはプラットフォーム標準の選択 UI を出しますが、**選択されたオフセットを JS 側から読む手段がありません**。API は正確なオフセットを必要とします（翻訳のアンカー、そして数式の途中で切れた選択の検出のため）。

文単位は仕様書 7 節の理想（任意範囲）より粗いですが、「分からない箇所だけ選択」は実際にはほぼ文単位ですし、ネイティブの不可視な選択にはない性質が 2 つあります: **各文が個別にスクリーンリーダーから到達可能**であること、そして **API に渡すオフセットが厳密**であること。

**Phase 2** 語単位の選択にはネイティブモジュールか WebView が必要です。文単位の選択オフセットは同じ API で通るので、置き換えは局所的です。

---

## D-019 Undo は「効果」と「痕跡」の両方を戻す

**決定** Undo は saved 行を消すだけでなく、その論文の impression も削除する。

**理由** フィードは表示済みの論文を除外します。impression を残したまま save 行だけ消すと、ライブラリからは消えているのに Discover には二度と現れない、という状態になります。ユーザーから見れば「取り消したのに戻ってこない」であり、仕様書 29 節の「Undoが機能する」を満たしません。

action ログは削除せず `undone` を立てて補償 action を追加するので、表示→スキップ→取り消し の履歴は分析用に残ります。

---

## D-020 action に単調増加の sequence 列を追加

**決定** `actions.sequence`（BIGSERIAL、migration 0002）を追加し、「直前の action」はこの列で決める。

**理由** `created_at` は `now()` を使っており、PostgreSQL ではこれは**トランザクション開始時刻**です。1 リクエスト内で 2 つの action を書くと同じタイムスタンプになり、順序が UUID のランダムな大小で決まってしまいます。Undo は何を戻すのか正確に知る必要があるので、これは許容できません。

---

## D-021 実行して初めて見つかった 4 件

いずれもテストが通っていた状態で、実際にアプリを起動して初めて判明したものです。記録として残します。

1. **`fetch` の receiver** — `this.fetchImpl = options.fetchImpl ?? fetch` としてプロパティに素で置き、`this.fetchImpl(...)` で呼ぶと、ブラウザは `this` がグローバルでない `fetch` 呼び出しを "Illegal invocation" で拒否します。この TypeError はリクエストの try 内で投げられるため、**全 API 呼び出しがユーザーには「オフライン」として見えていました**。回帰テストを追加済みです。

2. **`aria-hidden` の欠落** — 背面カードには `accessibilityElementsHidden` と `importantForAccessibility` を設定していましたが、react-native-web はどちらも `aria-hidden` に変換しません。ブラウザのアクセシビリティツリーに論文が 2 本入っていました（スクリーンリーダーが同じ画面で 2 本読み上げる状態）。3 つ目のプロパティとして `aria-hidden` を追加しています。

3. **Pan ジェスチャーがタップを飲む** — カード全体が Pan の対象なので、文のタップがジェスチャーに吸収されて選択が動きませんでした。`activeOffsetX/Y` を 12px に設定し、実際に動くまで Pan が起動しないようにしています（タップの取りこぼしが直るだけでなく、選択のつもりでカードが揺れるのも止まります）。

4. **Abstract が 1 文 1 行で切れていた（Phase 1-D で発見）** — 文ごとのタップ領域を作るために、各文を `flexDirection: 'row'` + `flexWrap: 'wrap'` の中の `Pressable` として並べていました。flex の子は自分の中で折り返さないので、**各文が折り返さない 1 行になり、カード幅で切れて 40 文字程度しか読めない**状態でした。仕様書 6 節でカードの本体は Abstract なので、これは表示の乱れではなく機能の欠落です。入れ子の `<Text>` に変更しました — 文ごとの `onPress` とアクセシビリティラベルを保ったまま、通常の行送りで折り返す唯一の構造です。**コンポーネントテストは文字列の存在しか見ないので、この種の不具合は通過します。**

**訂正（Phase 1-D）** ここには以前「3 の修正後も web ビルドでは文の選択が動かない」と書いていましたが、**これは誤りでした**。ブラウザで実際に確認したところ、文タップは選択ハイライトを付け、翻訳シートも開きます。当時そう判断したのは、シートが開いたかどうかを間違った文字列で探していたためです。**実機での確認は依然として必要です** — マウスのクリックはタッチのジェスチャーではなく、Pan と tap の競合が実際に起きるのはタッチ入力のほうなので、TASKS.md のチェック項目は残します。

---

## D-022 構造分類は `ai` ではなく `heuristic` と名乗る

**決定** キューフレーズによる規則ベースの Abstract 構造分類を実装し、その出力を `detected_by='heuristic'` として保存する。語彙 `detectionMethod` に `heuristic` を追加した（migration 0003）。

**理由** 仕様書 8 節は「AI分類の場合は自動検出ラベルを付け、原文を変更しない」と書いています。ラベルの要点は *どうやって付けたか* を読者に伝えることなので、キューフレーズ照合を `ai` と名乗らせると、そのラベルが持つ以上の信頼を主張することになります。仕様書 3 節の Trust by provenance に照らして、これは書き換えではなく名前の問題です。

**なぜ規則ベースを先に入れたか** カードの役割ラベル表示にも、Phase 2 の「Before you read」が method/result の文を見つけるのにも構造が要ります。学術 Abstract はジャンルとして定型性が高いのでキュー照合でもかなり当たり、コストはゼロで、オフラインで動き、そして何より **後からモデルを入れたときに比較対象になる**。fixture コーパスは `detected_by='source'` の正解ラベルを持っているので、この比較はテストとして常時回っています。

**実測** 60 件のコーパスに対して平均一致率 0.89、完全一致 34 件。テストは 0.80 / 25 件を下限にしてあります（キューの調整では落ちないが、実際の退行では落ちる水準）。

---

## D-023 復習は 2 択・固定間隔で、点数を付けない

**決定** 復習の答えは `again` / `got_it` の 2 つだけ。間隔は 3→7→16→35→70 日の固定はしごで、SM-2 のような ease factor は持たない。`again` は 0 に戻さず 1 段だけ下げる。

**理由** 仕様書 9 節と 10 節が繰り返し言っているのは「評価や罰ではなく」「派手な点数化はせず」です。SRS の一般的な設計（ease factor、連続日数、正答率）はどれも点数化で、しかも 1 日空けただけで失うものがある設計になりがちです。仕様書が求めているのは「数日後に表現を1件提示」であって、こなすべきキューではありません。

**`again` を 0 に戻さない理由** ほぼ覚えていたものを 1 回外しただけで過去の復習が全部消えるのは、まさに仕様書が排している「罰」です。1 段戻すだけなら、覚え直しのコストは払いつつ積み上げは残ります。

**キューの既定件数は 5** 総数は正直に返しますが、既定で見せるのは一握りです。

---

## D-024 Phase 2 のうち AI が要る部分は着手しない

**決定** 実翻訳 Provider、Why it matters、Before you read の AI 生成部分は Phase 2 から外し、AI なしで完成できる部分（構造分類・表現辞書・復習・保存理由）だけを実装した。

**理由** D-016 と同じ理由です。この環境は外向きのネットワークが遮断されており、LLM API の鍵もありません。プロンプトと Provider 実装を書くことはできますが、一度も実際の応答に当てられないものを「完了」として commit すると、動いていないコードが完了扱いで積み上がります。

**Provider interface は Phase 0 から存在します**（`ExplanationProvider`）。接続時に触るのは registry への登録 1 行と実装ファイル 1 つで、呼び出し側は変わりません。

---

## D-025 arXiv と OpenAlex のライセンス上の立場

**決定** arXiv API が返すメタデータ（タイトル・著者・Abstract・カテゴリ）は **CC0 1.0** として扱い、`license_id="CC0-1.0"` と `sourceTermsUrl=https://info.arxiv.org/help/api/tou.html` を全レコードに付けて保存する。OpenAlex は **データセット自体が CC0** であることを保存の根拠にしつつ、読者に示すライセンスは **論文ごとの `best_oa_location.license` / `primary_location.license`** から取る。知らないライセンス文字列は推測せず `license_id=None` / `abstract_redistributable=False` とし、ingestion はそのレコードの本文を取り込まない。

**なぜ 2 つのソースで扱いが違うのか** arXiv API のライセンスはメタデータ全体に一律で掛かるもので、論文ごとの投稿ライセンス（arXiv perpetual license, CC BY, CC BY-NC-SA など）とは別のレイヤーです。API から取れる範囲＝メタデータしか扱わない以上、判断すべきなのは前者だけです。OpenAlex は逆に、集約元の OA ロケーションごとのライセンスをそのまま持っているので、論文単位で答えられます。同じ「CC0」でも根拠が違うので、`license_id`（読者に示す値）と `raw.datasetLicense`（保存の根拠）を別のフィールドに分けています。

**知らないライセンスを推測しない** `KNOWN_LICENSES` にない文字列（fixture では `publisher-specific-oa`）は「たぶん再配布できる」とは扱いません。仕様書 21 節の「ライセンス不明の本文断片を扱わない」はここが実装箇所で、`abstract_redistributable=False` が ingestion のスキップ条件になります。ライセンス文字列そのものを保存して人間が後で判断する、という選択肢もありますが、判断されるまでの間そのレコードが「ライセンス欄が埋まっている」状態で存在するほうが危ういと考えました。

**やっていないこと 2 つ**

1. **出版社サイトのスクレイピングはしません。** 仕様書 21 節が明示的に禁じており、Provider は公式 API しか叩きません（`API_URL` は 2 つとも定数で、リダイレクト追従もしていません）。
2. **商用利用に向けた個別の利用規約レビューは未実施です。** 仕様書 21 節は「商用利用時は各ソースの規約を個別に確認」と書いていますが、これは法務判断であってコードで代行できるものではありません。**このリポジトリは研究・個人利用の前提で書かれており、商用配布の前に arXiv・OpenAlex 双方の規約を人間が確認する必要があります。** ここに書いているのは「確認したので安全です」ではなく「確認していないので、する必要があります」です。

**検証可能にしてある理由** ライセンスの主張は根拠 URL なしでは検証できないので、`sourceTermsUrl` をレコードに同梱しています（`test_the_licence_position_is_recorded_and_checkable`）。規約が変われば URL の先が変わるので、少なくとも「どこを見れば分かるか」は残ります。

---

## D-026 TypeScript にも lint と formatter を入れ、React Compiler の指摘は原則として直す

**決定** ESLint 9（flat config・`eslint-config-expo` 57 系）と Prettier 3 を導入し、`make lint` と CI の必須チェックに加えた。`eslint-config-prettier` を最後に置き、整形は Prettier だけが担当する。

**プロジェクト固有のルールを 2 つ足しています**

- `no-restricted-syntax` で **スタイル中の 16 進カラーリテラルを禁止**。仕様書 19 節はパレットを `@papermatch/design-tokens` の 1 箇所に置くと決めており、20 節の色覚に依存しない対比はそこが単一の出所であることに依存しています。実際に 1 件見つかりました（`AbstractCard` の `shadowColor: '#000'`）。ダークテーマでは黒い影はカード面に対して見えず、**「後ろにもう 1 枚ある」という唯一の手がかりが消えていました**。`color.shadow` トークンを両テーマに追加して置き換えています。
- `no-restricted-imports` で **画面どうしの相互 import を禁止**。画面はエントリポイントであって再利用単位ではないので、共有したくなったら `src/` に出す、というルールです。

**React Compiler 由来のルールで 13 件のエラーが出ました。うち 4 件は実バグでした。**

1. **`profile.tsx` の `Section` が render の中で定義されていた** — render のたびに別のコンポーネント型になるので、React は部分木を毎回 unmount して mount し直します。設定チップを 1 つ切り替えるたびにフォーカスが飛ぶ、つまり**スクリーンリーダー利用者はトグルのたびにセクション先頭へ戻される**状態でした。モジュールスコープへ出しています。
2. **`index.tsx` の選択範囲がカードをまたいで残りうる** — 選択は文のインデックスで持っており、`rangeToSelection` はそれをカード本文の文字オフセットへ変換します。カード切り替え時に effect で消していましたが、消えるまでの 1 render の間、**前の論文のインデックスが今の論文の本文に適用されます**。選択に paperId を持たせ、一致しなければ選択なしと見なす形に変えました（`selectionFor`、回帰テストつき）。effect で後から消すのではなく、そもそも成立しない構造にしています。
3. **`session.tsx` が `useMemo` の返り値を mutate していた** — token を保持するオブジェクトを `useMemo` で作って書き換えていました。React が memo を破棄すると値が黙って巻き戻ります。`useRef` に変更し、あわせて **どこからも読まれていない `token` state を削除**しました（読み手は `status` と `user` だけで、この state は再レンダリングを起こすだけでした）。
4. **`useDeck.ts` が render 中に ref を書いていた** — 破棄されうる render が書いた値を、非同期コールバックが「今のデッキ」として読む可能性がありました。effect での更新に変更しています。

**残した指摘 4 件と、その理由**

`react-hooks/set-state-in-effect` の 3 件（`saved` / `learn` / 翻訳シートのデータ取得）は、**ルールのほうが正しい**が、正しい直し方はデータ取得層の入れ替えです。これらは「mount 時に取得し、途中経過を state で持つ」という形で、React が勧めているのはまさにそれをやめて `@tanstack/react-query`（依存には既に入っています）に任せることです。オフライン時のキャッシュ・フォールバックが絡むので、lint 導入と同じ commit でやる作業ではありません。**行ごとの disable と理由コメントを置き、TASKS.md に移行項目として立てました。**

`react-hooks/refs` の 1 件（`session.tsx`）は**誤検出です**。ref を読んでいるのは `getToken` の中で、これはクライアントがリクエストを組み立てるときに呼ばれます。ルールはクロージャが *生成される* 場所しか見ないので render 中の参照に見えています。

**翻訳シートは閉じたときに unmount するようにしました** — 以前は mount したまま effect で state を初期化していました。unmount は同じ初期化を「間」なしで行うので、**別の選択でシートを開き直したときに、新しいリクエストが返るまでの数フレーム前の訳が出る**ことがなくなります。

**Prettier の対象から外したもの** Markdown と、手で表形式に整えた JSON（`tokens.json`、`fixtures/`、`generate-fixtures.mjs`）です。Prettier は表の桁を**文字数**で揃えますが、日本語の文字は等幅表示で 2 桁分あるので、揃えたはずの表がかえって崩れます。トークン表も 1 行 1 概念で light/dark を横に読む形が要点で、1 行 1 キーに展開されると比較ができなくなります。いずれもテストで担保しています。

---

## D-027 LaTeX は書き換えず、通すか拒むかだけを決める

**決定** `text/latex_safety.py` は **バリデータであってサニタイザではない**。危険な要素を取り除いた式を返すことはせず、その式をレンダラに渡してよいかどうかと、駄目な理由だけを返す。拒否された式も API のレスポンスには**そのまま乗る**。

**理由** 仕様書 11 節は「原式を勝手に変換しない」と書いています。一部を削った式は、**論文の式に見えるが論文の式ではないもの**で、原文を主役に置くと決めたアプリにとってこれ以上悪い出力はありません。そして 11 節は失敗時の挙動も決めています——「整形済み LaTeX ソースと原文リンク」。つまり拒否は「表示しない」ではなく「組版せずソースとして見せる」なので、文字列自体は送る必要があります。

**何を拒むか、なぜそれが危ないか** 式は WebView の中の KaTeX に渡ります。WebView はブラウザなので、式文字列はレンダリングエンジンに届く未検証入力です。

| 分類 | 例 | 危険の中身 |
| --- | --- | --- |
| 文書外への到達 | `\href` `\url` `\html*` `\includegraphics` | `javascript:` / `data:` URL はそのまま WebView 内でのスクリプト実行。`\html*` はクラス名・id・インラインスタイルをホスト文書に注入します |
| マクロ定義 | `\def` `\newcommand` `\csname` `\expandafter` | 展開爆弾（billion laughs）の書き方そのもの。KaTeX の `maxExpand` に頼るのは順序が逆で、拒否できる入力をレンダラ側の上限で受け止めることになります |
| ファイル・エンジン操作 | `\input` `\write` `\catcode` | KaTeX には無意味ですが実際の TeX エンジンには意味があり、このテキストはいつかそちらに渡るかもしれません |
| レイアウト爆弾 | `\rule{1pt}{99999em}` | 画面が固まる高さの箱。**コマンドではなく寸法**を上限にしています（`\rule` 自体は正当な組版です） |

**判定はサーバで行います。** 仕様書 25 節に従い、「この文字列を自分のレンダラに渡してよいか」をクライアントに判断させません。クライアントが 1 回のリリースで間違えれば終わりです。API は `renderable` と `refusalReasons` を返し、クライアントはそれに従います。

**意図的に安全側へ倒している点** `a \\href` は本来「改行 + href という文字列」ですが、2 つ目のバックスラッシュを `\href` の開始として読み、式を拒否します。誤検知の代償は式 1 本がソース表示になることで、見逃しの代償は WebView でのスクリプト実行です。通常の書き方（`\\` の後に空白）は影響を受けません。

---

## D-028 「検証済み」を fixture に書かせない

**決定** 手動作成の数式カード（`fixtures/math-cards.json`）は **`verificationStatus` を一切持たない**。持つのは*チェックそのもの*——数値的に標本化する恒等式と、次元の釣り合い——で、状態は取り込み時に `papermatch_api.mathcheck` を実際に走らせた結果を保存する。

**理由** 仕様書 12 節は 6 つの検証状態を定め、「未検証の変形は既定で非表示」と決めています。ここで fixture に `"verificationStatus": "mechanically_verified"` と書けるようにしてしまうと、「検証済み」は**誰かがそう入力した**という意味にしかなりません。それは 12 節が避けようとしていることそのものです。テストで、`cards` 配下に検証状態の文字列が現れないことを確認しています。

**チェックの中身と、その限界** CAS はありません（この環境に sympy はなく、外部にも出られません）。やっているのは、**LaTeX と並べて著者が書いた機械可読な形**を標本評価することです。式の両辺を通常の式として書き、変数と範囲を宣言し、それを 25 点で比較します。誤った変形はほぼすべての点で食い違うので、これは実際に効くチェックです。ただし弱点が 2 つあり、どちらも隠さずに書いておきます。

1. **宣言した範囲でしか主張していません。** 範囲は主張の一部なので、evidence に記録して保存します。
2. **チェックが、隣にある LaTeX と食い違っていても検出できません。** これは LaTeX を解析しない限り閉じない穴です。それでも得られるのは、「検証済み」と書かれた step には*何か機械的なことが実際に起きた*という保証です。

**空虚なチェックへの対策** 変異テスト（片辺を摂動して失敗するか）だけでは不十分でした。トートロジー（`A` と `A` を並べ替えただけ）は摂動すれば壊れるので変異テストを通ってしまいます。**実際、最初に書いた 2 件がそれでした。** そのため両辺が空白除去後に同一なら落とすテストを追加し、該当した step は書き直しました。

- ガウス積分の 1 段目は「2 次元ガウシアンが 1 次元 2 つに分解する」という*点ごとの恒等式*に置き換え
- 2 段目は極座標代入そのもの（`x = r cosθ, y = r sinθ` を入れて `r` だけの関数になること）を標本化
- 3 段目は原始関数を差分商で微分し、被積分関数と一致するかを確認

**チェックできないものは、チェックしたことにしない。** フォン・ノイマン エントロピーの対角化 step の中身は「トレースが基底に依らない」という線形代数の事実で、標本評価では確かめられません。ここにトートロジーを書けば `mechanically_verified` が取れてしまうので、**数値チェックを付けず次元チェックだけにして `dimensionally_checked` に留めています。**

**`combined_status` が返さないもの** `source_exact` と `human_reviewed` は「どこから来たか」「誰が見たか」の主張で、どれだけ計算しても得られません。関数の返り値からこの 2 つを除外し、テストで固定しています。このコーパスは誰もレビューしていないので、カードの `review_status` はすべて `draft` です。

---

## D-029 KaTeX はアプリに同梱し、数式は「データ」としてだけ WebView に渡す

**決定** KaTeX の JS・CSS・フォントをビルド時に 1 つのモジュール（`apps/mobile/src/math/katexRuntime.ts`、約 650KB）へインライン化し、数式は `<script type="application/json">` に入れて `textContent` から読む。**LaTeX を HTML へ文字列結合しない。**

**なぜ同梱か** CDN は 2 つの理由で使えません。仕様書 26 節はオフライン動作を求めており、電車の中で数式が組めないアプリは要件を満たしません。そして仕様書 25 節に照らすと、レンダリング時に外部から実行可能コードを取ってくること自体が避けたい依存です。フォントは woff2 のみを入れています（KaTeX は woff2 / woff / ttf を全て同梱しますが、WKWebView と Android System WebView はどちらも何年も前から woff2 に対応しており、3 形式抱えると容量が 3 倍になるだけです）。使わない形式の `@font-face` は**残さず削除**しています — 残すと、無いファイルを取りに行く静かなネットワークリクエストになります。

**なぜ「データとして」が絶対条件か** 数式は WebView の中の KaTeX に渡ります。WebView はブラウザなので、**マークアップとして届いた数式文字列はスクリプト実行**です。`JSON.stringify` だけでは足りません — `</script>` を含むペイロードは、引用符をどう escape してもそこでブロックを閉じます。ブラウザは JS エンジンに渡す前に `</script` を探すからです。そのため `<` `>` `&` を `\uXXXX` 形へ、さらに JSON では有効だが JavaScript では行終端子になる U+2028 / U+2029 も escape しています。

**同じものを 3 重に止めています。** この 1 コンポーネントだけが未検証の内容を実行するので、独立した層を重ねました。

1. **サーバ**が `\href` や `\html*` を拒否する（D-027）
2. **KaTeX の設定**が `trust: false` でそれらを無効化する（サーバが拒否していない前提で構成する）
3. **Content-Security-Policy** が `default-src 'none'` であらゆるロードを禁じ、**`onShouldStartLoadWithRequest`** が最初以外のナビゲーションを拒否する（仕様書 25 節）

**実ブラウザで確認したこと** 単体テストは文書の *文字列* を見るだけなので、生成した文書を Chromium に実際に読ませて確認しました。同梱コーパスの 18 本すべてが組版され全てに MathML があること、`</script><script>window.__alerted=1</script>` という数式では **script 要素が 3 つ（こちらの分だけ）で注入スクリプトは走らない**こと、壊れた式は LaTeX ソースにフォールバックし `{ok:false, error}` を post すること、Dynamic Type が効くこと、そして**ネットワークリクエストが 0 件**であること。

**MathJax へのフォールバックは実装していません。** 仕様書 11 節は KaTeX → MathJax → ソース表示という 3 段を挙げていますが、MathJax の同梱はさらに約 1MB で、KaTeX が扱えない構文の大半はサーバ側で既に弾いています。そして**信頼の観点で効いているのは 3 段目**（ソースを見せる）です — 読者が原文と突き合わせられる状態を保つことが要点で、そこは実装済みです。MathJax は TASKS.md に残しました。

**未確認の点** WebView コンポーネント（`MathView.tsx`）自体は**この環境で実行できていません**。react-native-webview は web ビルドで動かず、実機もありません。文書生成側は純関数として全てテストし実ブラウザで描画確認していますが、**高さの通知・ナビゲーション拒否・アクセシビリティは実機確認が必要**で、TASKS.md のチェックリストに入れています。

---

## D-030 依存バージョンは Expo Go が持つネイティブに合わせる。ただし JS は別

**きっかけ** 実機（Android / Expo Go）でアプリがスプラッシュから進まず、「PaperMatch」の文字だけの白画面で止まりました。バンドルは成功しており、赤いエラー画面も出ません。

**原因** `react-native-gesture-handler` を `~3.1.0` で入れていましたが、**Expo Go SDK 57 が内蔵しているネイティブは 2.32** です。`GestureHandlerRootView` はアプリ全体を包んでいるので、ここが噛み合わないと何も描画されません。さらに悪いことに、当時のツリーには **2.32 と 3.1 の両方**が入っていました（元の指定が hoist されて残っていた）。gesture-handler が 2 つあるのは、それ単体で壊れる状態です。

これは D-014 の付けです。`npx expo install` が proxy で 403 になるため SDK 57 のバージョンを手でピン留めしましたが、いくつか外しました。**正解は最初から `node_modules/expo/bundledNativeModules.json` にあり**、ネットワーク無しで読めました。今後バージョンを決めるときは、まずこのファイルを見ます。

**合わせるもの / 合わせないもの** ネイティブ側を持つパッケージ（`react-native-*`、`expo-*`、`@react-native-*`）は Expo Go が持っているものと一致させます。一方 **`react` と `react-dom` は JavaScript** で、バンドルしたものがそのまま動くので、Expo Go と一致させる必要はありません。ここも一度合わせに行って、`react-test-renderer` との組み合わせでテストが 23 件落ちました（`Invalid hook call`）。**「Expo Go に合わせる」はネイティブに対する要求であって、JS ライブラリの話ではありません。**

**併せて見つかった無限ループ** バージョンを揃えてアプリが起動するようになった直後、`Maximum update depth exceeded` が出ました。原因は 2 つ重なっていました。

1. `useDeck` が**毎レンダリングで新しいコントローラオブジェクト**を返しており、Discover 画面の effect が `[current, deck]` に依存していた。つまり effect が毎レンダリング走る。
2. その effect の中身は `noteImpression` の dispatch で、`impression_recorded` は**同じカードの重複でも必ず新しい state を返していた**。

結果として dispatch → 再レンダリング → effect → dispatch のループになります。1 は `useMemo` と、effect の依存を「コントローラ全体」から「記録関数」に絞ることで、2 は**何も変わらないときは受け取った state をそのまま返す**ことで直しました。2 のほうが本質的です — **変化のないアクションに対して等価な新オブジェクトを返す reducer は、state を見ている effect をすべてループの種にします。** 回帰テストは同一性（`toBe`）で書いています。

---

## D-031 詳細度スライダーは「説明の量」ではなく「導出の量」を選ぶ

**決定** Focus Mode の 最短 / 標準 / 一行ずつ / 初学者向け は、**カードが持っている導出のうちどれだけを見せるか**を選ぶもので、説明を生成し分けるものではない。どのレベルも、保存されていない step を作り出さない。

**理由** 仕様書 10 節のこの 4 段を「説明の詳しさ 4 段階」と読むこともできますが、そう作ると **最も詳しいレベルは必ず何かを捏造することになります** — カードには step 単位より細かいものが存在しないからです。実際に段階で変わるのは (1) 個々の操作を出すかどうか、(2) 「なぜ?」を最初から開くかどうか、の 2 つだけです。テストで「どのレベルでも、表示される step は保存済み step の部分集合である」ことを固定しています。

**最短の形を作り直しました。** 最初の実装は step を間引いただけで、式は 4 本すべて残り矢印だけが消えました。**途中に説明のない穴が空き、レンダリングの不具合にしか見えません。** いまは両端の式だけを出し、`途中の 3 手を省略` と件数を明記します。省略していることを言うのが、同じ意図の正直な形です。

**理解チェックはカードから作り、生成しません。** 仕様書 10 節の 次の一手 は、保存済みの導出だけで答えを作れます。誤答の選択肢は**同じカードの他の操作**です — 無関係な導出から持ってくると語彙だけで弾けてしまい、何も試していないことになります。同じカード内なら全部もっともらしいので、「ここで効くのはどれか」を分かっている必要があります。結果は文であって点数ではありません（10 節: 派手な点数化はせず）。返り値に `correct` と `messageKey` 以外を持たせないことをテストで固定しています。

**極限タブは実際に走ったチェックだけを報告します。** step の evidence に入っている「標本化した範囲」と「次元の釣り合い」をそのまま出し、`ここに書かれていない範囲については何も主張していません` と添えます。evidence を持たない step については何も言いません（未検証の変形に適用範囲を書けば、誰もやっていないチェックを説明することになります）。

---

## D-032 探索ボーナスは置かない。興味の項は分野の重みで刻む

**決定** 仕様書 16 節がスコアの構成要素として挙げている **探索ボーナスを実装しません**。代わりに、興味一致の項を「選んだ強さ × その分野が論文に占める重み」の連続値にしました。

**探索ボーナスを置かない理由** D-017 で 70/20/10 は**枠の配分**だと決めています。候補は 3 つのプールに分けられ、**プールごとに独立に並べ替えられます**。したがって「どのプールにいるか」だけで決まる項は、比較相手全員に同じ値が乗るので、**1 枚のカードも動かせません**。重みとテストと説明文を持ち続けて、証明できる範囲で何もしない算術を抱えることになります。探索プールが実際に必要としているのは「いつも同じ数本が出てこないこと」で、これはプール**内**のタイブレークです。ページングの seed に依存するので `feed.py` 側に置き、`breakdown` には `tie_break` として別名で出します — スコアの構成要素ではないからです。

**興味の項を刻んだ理由** これは危うく落とすところでした。書き上げた `interest_match` は「選んだ分野なら 1.0 / 隣接なら 0.5 / それ以外 0」という**所属判定**でした。ところが枠配分の下では、**matched プールのカードは全員が定義上 1.0 になります**。つまりモデルで最も重い項が、実際の並び順に対して何も言わなくなる。差し替え前の `feed.py` のインラインの式は `interest.strength * max(0.2, weight)` を使っており、こちらは論文ごとに違う値でした。**素直に差し替えていたら、信号を減らしながら「スコアラーを導入した」と報告することになっていました。**

いまは 1 本の式です。宣言された強さ（隣接分野は 0.5 を継承）に、その分野が論文に占める重みを掛けます。重みは先に 0.15 でふるいます（付随的なタグは主題ではない）が、通ったものには 0.2 の下限を置きます — 一部が選んだ分野である論文は、やはり選んだ分野の論文だからです。

**level 0 の扱いは移植しました。** 仕様書 18 節の level 0 は 数式を表示しない で、これは重み付けで釣り合わせる好みではありません。数式の多い論文はこの項で 0 になります。ただし除外はしません — 数学系の分野と level 0 を同時に選んだ読者のデッキが空になります。テストは固定値（`< 0.6`）ではなく **level 3 のときの同じ論文と比べる**形に書き直しました。固定値のしきい値は、level 0 が意味を失うような重み変更のあとも通り続けます。

**推薦理由とスコアの内訳は別物です。** `explain()` が返すのは `interest` や `freshness` といった**項の名前**、カードに出る理由は `enums.json` の固定語彙（`matches_field` / `adjacent_field` / `similar_to_saved` / `recent` / `foundational`）で、2 言語の文になります。スコアラーは並び順だけを決め、理由のラベルはそのまま残しました。両者が交わらないことをテストで固定しています。

---

## D-033 仕様書 16 節のフィードバック 5 つのうち、3 つは語彙自体が無かった

**見つけたこと** 16 節はフィード調整として 5 つを挙げています。このうち `hide_topic` と `hide_author` は初期スキーマからありましたが、**類似論文を減らす / 実験系を増やす / 古典的論文を増やす には対応する action type が存在しませんでした**。ボタンを置いても送り先が無い状態です。

**決定 1: 設定列ではなく action にする。** 3 つとも `actions` に記録し、フィードは「取り消されていない・30 日以内」の件数だけを読みます。16 節が否定的フィードバックを「今回は見送る」と扱えと言っている以上、**Undo と時間経過の両方で戻る**必要があり、このスキーマはすでに両方を知っています（D-019）。設定列にすると、取り消しも失効も自前で書くことになります。副次的な利点として、読者は自分が押したものを活動履歴でそのまま見られます — フィードの形が変わったことから逆算する必要がありません。

**3 回で飽和させ、そこで止めます。** 1 回はヒント、3 回で最大。20 回押しても永久追放にはなりません。「嫌い」と決めつけないというのは、そういう意味だと読みました。

**決定 2: `less_similar` は加算ではなく倍率。** スコアラーがすでに計算した類似ペナルティに掛けます。直前に見たものと似ていない論文にはペナルティが 0 なので、**何も起きません**。一律に引くと、一部の重複のために全カードが罰を受けます。値が 0 の項は `breakdown` に載せません — `less_similar: -0.0` が全カードに付くのは「ここで効いている」と読めてしまい、嘘になります。

**決定 3: 「実験系」を判定する分類器を書きました（`services/method_kind.py`）。** arXiv も OpenAlex も理論/実験の区別を公開していないので、無ければこのボタンは押しても何も起きないものになります。D-022 と同じ立場で、**規則ベースだと名乗ります**（`ai` ではない）。

**棄権します。** 200 語の要旨で判断がつかない論文は多く、そこでの正直な出力は**ラベルを付けないこと**です。無理に選ばせれば、難しい半分を確信を持って間違え、フィードはその間違いに従って並べ替えます。どちらのラベルも持たない論文は「判定していない」であって「どちらでもない」ではありません。ナッジ側も**実験系と分かっているものを持ち上げる**だけで、他を押し下げません。

**手掛かりは「何をしたか」であって主題ではありません。** `quantum` は証拠になりません。`we measured` `our apparatus` はなります。ここを外すと、分野分類器が別の名前を名乗っているだけのものになります。

**自分のテストに 2 つ直されました。** (1) 導出と測定が両方書かれた要旨が `theoretical` に倒れました。原因は `perturbative` と `non-perturbative` を**両方**列挙していたことで、1 語が 1.8 点になっていました。手掛かりが互いの部分文字列にならないことをテストで固定しています。(2) `we collected` としか書いておらず、**現在形の要旨を全部取り逃していました**（`calibration` と `calibrated` でも同じ間違いをしていました）。語幹に直しました。

**分類器を fixture に合わせて調整はしていません。** 直したのは上の 2 つの実際の欠陥だけで、そのあと出た結果をそのまま報告します。49 本中、実験系 2・理論 5・レビュー 12・**判定なし 30**。棄権率が高いのは設計どおりです。

**決定 4: ナッジの Undo はカードを呼び戻しません。** `skip` や `save` の Undo は、消えたカードをデッキに戻す必要があります。しかし「実験系を増やす」は**配合の要求であってこの論文を返せという要求ではなく**、すでに通り過ぎたカードが復活するのは頼んでいない変更です。`hide_topic` / `hide_author` は従来どおり戻します（UI がカードを送るのと同時に投げるため、ジェスチャ全体の取り消しになる）。

**UI は 5 つを 1 枚のシートに置き、押しても閉じません。** 「この話題を減らして、古典を増やす」は 1 つの考えなので、1 タップごとに閉じると 2 往復になります。**効かない行は理由を書いて無効化します** — 著者名の無い論文で「この著者を表示しない」が成功を報告して何もしないのは、無効化より悪いです。文言は日英とも「嫌い」「興味なし」を含まないことをテストで固定しました。

**シートを実際にブラウザで開いて見つけたこと。** 著者の行は「K. Grabowski の論文を…」と**名前を書く**のに、話題の行は「この分野のカードを…」としか書いていませんでした。シートはカードをほぼ覆うので、**開いた時点で読者はどの分野を抑えようとしているのか見えません**。分野 id をカードのチップと同じ表記で行に書くよう直しました。テストで両方が名指しされることを固定しています。

**もう 1 つ、これは環境の話。** 最初のブラウザ確認では action の POST が 422 で落ちました。原因はコードではなく、**`enums.json` を編集する前に起動していた uvicorn が古い語彙を保持していた**ことです。語彙は import 時に読まれるので、`enums.json` を触ったらサーバを再起動する必要があります。ただしこの失敗は、**シートが黙って何もしないのではなく「送れませんでした」と文で報告する**ことを実際に証明しました — 設計どおりに動いた失敗です。

---

## D-034 「いつ取得したか」と「いつ確かめ直したか」は別の列

**決定** `papers` に `last_refreshed_at` を足し、再取得キューの並び順はこちらで決める。`acquired_at` は使わない。

**きっかけ** worker を書いて動かしたら、1 回まわしたあとも **47 件すべてが「古い」ままキューに残っていました**。2 回目のパスの仕事量が 1 回目とまったく同じで、初めて気づきました。

**原因** キューを `acquired_at` で並べていました。ところが `acquired_at` は**仕様書 21 節が定める出自**で、値は provider が寄こします。`upsert_record` はそれをそのまま書き戻すので、**取得時刻を返さない provider（fixture を読む mock がまさにそれ）では、何度取得し直しても値が動きません**。結果、キューはいつまでも最も古いバッチを再チェックし続け、それより新しい論文には永久に到達しない。しかも `checked=47 unchanged=47` という**健全に見える数字を出し続けます** — だから読んでいるだけでは見つからなかったし、他のテストでも捕まりませんでした。

**直した形** 「いつ取得したか」（出自・provider が言う）と「いつ確かめ直したか」（運用・こちらが書く）は**別の問いなので、別の列**にしました。キューは `coalesce(last_refreshed_at, acquired_at)` で並べます。**provider が解決できなかった論文にも `last_refreshed_at` を立てます** — でないと今度はその 1 件がキューの先頭に居座り、後ろ全部を止めます（同じデッドロックの裏返し）。

回帰テストは、直す前の実装に戻すと**実際に落ちること**を確認してあります。

**併せて固定したこと。** provider が返さなくなった論文を**撤回扱いにはしません**。`get_by_canonical_id` の `None` は「provider がくれなかった」であって、一時障害・識別子の移動・そもそも持っていない、のどれでもあり得ます。これを撤回と読むと、**上流の不調 1 回で読者のフィードが静かに空になります**。件数は数えて監査に残しますが、`retraction_status` には触りません。

---

## D-035 Abstract の数式は組版する。カードは 1 枚に 1 つの文書

**きっかけ** デモのスクリーンショットを見ていて指摘されました。Discover カードの本文に
`$\mathbb{P}(|f - \mathbb{E}f| > t) \le 2\exp(-ct^2/\|f\|_{\mathrm{Lip}}^2)$` が
**LaTeX ソースのまま**出ていました。

**規模** 調べたら、このコーパスの Abstract の **95%** がインライン数式を含んでいました（1 本あたり平均 2.4 か所）。つまり例外ではなく、**ほぼ全部のカードが壊れて見えていた**ということです。仕様書 11 節は「インライン」を明示的に要求しており、LaTeX ソースの表示は**失敗時のフォールバック**です。常時フォールバックの状態でした。

**文字への置き換えは選びませんでした。** 数式を Unicode に写すやり方も考えましたが、実際のデータを見ると **18%（137 か所中 25 か所）が `\frac` / `\sqrt` / `\sum` / `\int` / `\binom` を含み、2 次元の組版を必要とします**。組める式と組めない式が混在した表示は、一貫していないぶん今より分かりにくくなります。それに、この repo は式について「勝手に変換せず、できないなら拒む」で通してきました（D-027・D-028）。

**カード 1 枚につき文書 1 つ。** 式ごとにレンダラを置くのではありません。仕様書 7 節の部分翻訳は**文単位で選ぶ**ので（D-018）、文は 1 つのタップ対象のままでなければなりません。式ごとに分けると、文が「まとめて選べない破片」になり、さらにカード 1 枚に何個もレンダラが載ります。いまは文が `role="button"` の span 1 つで、タップすると index を post します。

**後ろのカードと、数式の無い Abstract にはレンダラを置きません。** 後ろのカードは縮小・減光された予告でタップもできないので、読めないものを組むために払うコストです。数式が 1 つも無い Abstract（約 20 本に 1 本）も同じ理由で素通しです。

**平文の経路は消していません。** 文書が高さを報告するまで平文が出て、報告が来なければそのまま残ります。**レンダラが失敗したときに読者の手元に残るのが「さっきまでの Abstract」であって「空のカード」ではない**、という状態にしたいからです。この画面は、失敗しても後ろに何もありません。

**web は iframe、しかも `srcdoc` ではなく blob URL。** `react-native-webview` は web で動かないので、そのままでは web ビルドで Abstract の位置に「does not support this platform」と出ます。**ブラウザで検証できる唯一の画面がそれでは困ります。** `srcdoc` は親のオリジンを継ぐため文書の meta CSP 下でインラインスクリプトが走らず、KaTeX が黙ってソース表示に落ちます（置き換えようとしていたバグそのもの）。blob URL なら文書が自分のオリジンを持ち、CSP が文書に適用されます。frame は sandbox するので、選択の反映は「関数を呼ぶ」ではなく postMessage です。

**実ブラウザで確認したこと** 式が組まれること、MathML が併記されること、文をタップすると翻訳シートが開くこと、選択が色と下線の両方で付くこと、**`\href{javascript:...}` が `javascript:` リンクを 1 つも作らないこと**、外部リクエストが 0 件であること。

**書体を間違えて 1 回作り直しました。** 最初 iframe に sans のスタックを直書きしたら、**カードの中で Abstract だけが違う書体**になり、レンダリングの不具合にしか見えませんでした。いまはトークンの `abstract` ロール（セリフの読み書体）をそのまま渡します。**式を `nowrap` にしたのも見て直したところ**で、折り返しを許すと式の途中で改行され、文末の句点が次の行頭に取り残されていました。

---

## D-036 CI と同じものを 1 コマンドで回す（`npm run ci`）

**きっかけ** push してから CI が落ちたことが 2 回続きました。どちらも**環境差ではなく、私がコマンドの一部しか実行していなかった**だけです。

1. `npm run lint` は流したが `npm run format:check` を流さなかった。CI の lint ステップは 2 つを続けて実行しており、落ちたのは後者でした。
2. `tsc` を**テストファイルを書く前**に流し、書いたあとに流し直さなかった。`noUncheckedIndexedAccess` が配列アクセス 2 箇所を弾きました。

どちらも手元で 1 分の作業です。**「知っているのにやらなかった」を意志で直すのは無理なので、順番ごと 1 つのコマンドにしました** — `scripts/ci-local.sh`（`npm run ci`）が `.github/workflows/ci.yml` と同じものを同じ順で回します。PostgreSQL が要る 2 つ（pytest と migration 適用）は `--fast` で外せます。

**ワークフローにしか書かれていないチェックは、push して初めて存在を知るチェックです。** ci.yml を変えたら、このスクリプトも変えます。

---

## D-037 保存理由チップは「保存のあと」に出す。既定は選択肢にしない

**決定** 右スワイプは**その場で保存を確定**し、理由チップはそのあとの Undo トーストに並べる。既定の `interesting`（気になる）は**チップとして出さない**。

**理由** 仕様書 9 節は「右スワイプの既定は『気になる』。**任意タグ**：…」と書いています。つまり保存は理由の入力を待ちません。先にシートを出して理由を聞くと、**1 つのジェスチャーが 1 つの判断に変わり**、カードをめくっている読者はめくるのをやめます。6 節も 保存理由チップを**任意表示** です。

既定をチップにしないのも同じ理由です。それはスワイプがすでに意味したことで、選択肢として並べると「何か選ぶまで保存は確定していない」と読めてしまいます。

**Undo と同じ面に置きました。** 「保存しました / 取り消せます / 理由も付けられます」は 1 つの出来事についての 1 つの面です。別々のシートにすると、2 つの一時的な UI が同じ場所を取り合います。

**サーバが確認するまでチップを出しません。** まだ受け付けられていない行に `PATCH /saved/{id}` を送ることになるからです。Undo が `actionId` を待つのと同じ条件で、判定も同じ 1 か所です。

**チップの一覧は共有語彙から既定を引いて作ります。** 手で書き写すと `enums.json` にタグを足したとき、**どこにでもあるのに読者が選べる画面にだけ無い**という状態になります。テストで「既定 + チップ = 語彙全体」を固定しました。

**`PATCH` は置き換えなので、常に完全な集合を送ります。** `toggleReason` が既定を必ず含めて返すのはこのためで、落とすとサーバ側でも落ち、読者が触っていない「気になる」フィルタから論文が消えます。

**楽観的に光らせて、失敗したら戻します。** タグは小さい主張なので往復を待つと壊れて見えます。ただし**サーバが拒んだチップが光ったまま**にはせず、元に戻して文で伝えます。

---

## D-038 読者の「報告」と、レビュアーの「判断」は別のテーブル

**決定** `content_reports` を新設する。既存の `review_events` は使わない。

**理由** `review_events` の語彙は `approved` / `rejected` / `needs_changes` で、これは**その判断を下す立場にある人**の語彙です。読者が持っているのは「これは正しくないと思う」であって、その先どうなるかを決める権限ではありません。同じテーブルに入れると、**匿名のゲストが `approved` を主張できる**ことになります。

**報告は `verification_status` を動かしません。** あの列は「どの機械的チェックが走って、何を返したか」の記録です（`mathcheck/`）。読者の意見はチェックではありません。動かせるようにすると、**検証済みしか入らないことだけが価値である列に、誰も検証していない主張が入ります** — しかも `numerically_spot_checked` が下がっているのを見た読者に、それをやったのがチェッカーなのか通りすがりなのか区別する手段がありません。

**カードも隠しません。** 1 人の報告は全員から取り上げる根拠になりません。それは判断であり、判断は `review_events` の側です。報告がするのは、**判断する人がそのカードを見つけられるようにすること**だけです。

**1 人 1 対象 1 理由につき 1 件。** 27 節の指標は**率**なので、同じ読者が 2 回叩いたら 2 人になってしまいます。2 回目は上書きで、行は増えません。ただし**補足が空の再送で、先に書かれた補足を消しません** — 最初の実装は無条件に上書きしていて、同じチップをもう一度押した瞬間に読者が書いた文が消えました。「言い足せる」と書いたコメントと逆のことをしていたので直しました。

**報告先は「カード」ではなく「対象」です。** `equations` と `derivation_steps` は `provenance_kind` を持つので、**論文由来のものとモデルが書いたもので報告数を分けられます**。27 節のガードレールは「AI 説明の問題報告率」で、総数だけでは意味を成しません — 分けて初めて「モデルの書いたもののほうが苦情が多いか」が言えます。

**文言が約束しないこと。** このビルドでは誰もキューを見ません。だから「確認します」とは書かず、**実際に起きたこと（記録した）だけ**を書きます。シートの冒頭に「報告しても、このカードの内容や検証状態は変わりません」と先に出すのも同じ理由です — 報告で直ったと思った読者は、確かめるのをやめます。

---

## D-039 算出できない指標は、落とさずに「算出できない」と出す

**決定** 27 節の指標を `services/metrics.py` に実装し、`papermatch metrics` で出す。**データが無くて計算できないものは、省略せず値なしで並べる。**

**理由** 12 個の数字が並んだレポートは完成して見えます。12 個の数字と「これは算出できない、足りないのはこれ」の 3 行が並んだレポートは、**どこが見えていないか**を教えてくれます。いま算出できないのは 3 つで、どれも省くと存在ごと忘れられます。

- **数式カード完了率** — カードを「完了した」と記録するイベントがそもそも無い。
- **Canvas から論文詳細への遷移率** — Canvas は Phase 6 で、測る対象の画面が無い。
- **クラッシュ率・ジェスチャー失敗率** — クライアント計測が無い。

**分母は全部書きます。** ここで作れる間違いのほとんどは分母の間違いだからです。「10 秒以上読まれた割合」を**全 impression**で割ると、**クライアントが dwell を報告し損ねたときに数字が下がります** — いちばん動いてほしくないときに動く。だから分母は「dwell が記録された impression」で、レポートにもそう書きます。同じ理由で、分母が 0 のときは `0.000` ではなく**値なし**にします。0.000 は「誰もやらない」と読めますが、実際は「まだ誰も機会を得ていない」です。

**セッションの定義は仕様書に無いので、こちらで決めました。** North Star は「セッション数」を数えますが、セッションの定義がなく、アプリはセッション id を記録していません。**30 分の無操作で区切る**ことにし、定数にコメントとして理由を書きました（電話 1 本は跨げて、朝と夜が 1 つにならない長さ）。

**「自力で読み」の読み方も決めました。** 「翻訳を使わずに」とは取りません。**選択範囲の翻訳はプロダクトが働いているのであって、読者が失敗しているのではない**からです。9 節は「翻訳なしで読んだ割合」を別の数字として持っているので、仕様書自身が両者を分けています。

**走らせて自分のガードレールのバグを見つけました。** 「AI 説明の問題報告率」を **derivation step だけ**で数えていて、報告が入っている DB に対して 0 を返しました。読者が「説明が誤っている」を押すと**カードに対して**報告が立ち、カードにも `provenance_kind` があります。**ガードレールとしては最悪の壊れ方**です — 静かで、しかも安全に見える方向に間違える。いまはカードと step の両方を数えます。回帰テストは、直す前に戻すと落ちることを確認済みです。

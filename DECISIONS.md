# Decisions

仕様書 0 節の指示に従い、仕様に書かれていない点は勝手に削らず、合理的な既定値を採用してここに記録します。各項目は「決めたこと / 理由 / 見送った案 / 見直す条件」で書きます。

---

## D-001 API は FastAPI（Python）

**決定** 仕様書 22 節が挙げる FastAPI と NestJS のうち FastAPI を採用。

**理由** 後続フェーズの重い部分がすべて Python 側にあります。Phase 5 の数式検証は SymPy / 次元解析、Phase 3 の埋め込みとクラスタリングは numpy / scikit-learn、Phase 5 の LaTeX 解析も Python のエコシステムが厚い領域です。API を TypeScript にすると、これらを別サービスに切り出して言語間の境界を 1 つ増やすことになります。

**見送った案** NestJS。モバイルと言語が揃う利点はありますが、上記の処理系を worker として別に持つ必要があり、Phase 5 で必ず払うコストが増えます。

**見直す条件** 数式パイプラインを最初から独立サービスにすると決めたとき。

---

## D-002 語彙は 1 つの JSON を両言語が読む

**決定** `packages/shared-types/enums.json` を唯一の定義とし、Python は実行時に読み込み、TypeScript はリテラル型として再掲したうえでテストで一致を検証する。

**理由** 「保存理由」「検証状態」「provenance の種別」のような語彙は、クライアントとサーバとデータベースの 3 か所に現れます。3 か所で独立に書けば必ずずれ、しかも ずれた瞬間ではなく 数週間後に本番で気づくことになります。1 か所を正とし、ずれをテストで落とすほうが安いと判断しました。

**見送った案** OpenAPI からのコード生成。生成物のレビューとビルド順序の管理が必要で、Phase 0 の規模に対して重すぎます。API の型が増えたら再検討します。

**見直す条件** 語彙以外の型も共有したくなったとき（そのときは OpenAPI 生成に移行）。

---

## D-003 列挙型は VARCHAR + CHECK 制約（PostgreSQL ENUM を使わない）

**決定** 語彙列は `VARCHAR` にし、`enums.json` から生成した `CHECK` 制約を付ける。

**理由** ネイティブ ENUM に値を足すには `ALTER TYPE` が要り、削除や並べ替えはさらに面倒です。この仕様書の語彙は今後まず間違いなく増えます（保存理由、数式カード種別、推薦理由）。`CHECK` なら制約の張り替えだけで済み、しかも定義を共有ファイルから生成できます。

**代償** DB 側の型安全性はやや弱くなります。これは `tests/test_vocab_parity.py` が制約と共有語彙の一致を検証することで補っています。

---

## D-004 スキーマは Phase 0 で全エンティティを作る

**決定** 仕様書 23 節の全 26 テーブルを最初のマイグレーションで作る。Phase 4–6 でしか使わない `equations`、`math_cards`、`canvas_positions` も含む。

**理由** あとから列を足すのは、既存行のバックフィルとデフォルト値の決定を伴います。空のテーブルが 10 個ある costs はほぼゼロで、リレーションの形を最初に一度考えておくほうが、フェーズごとに考え直すより設計が一貫します。

**代償** 使われていないテーブルを見た人が混乱する可能性。ARCHITECTURE.md の表でどのフェーズが使うかを明示しています。

---

## D-005 サンプルデータは合成テキスト

**決定** 実在論文の Abstract を同梱せず、分野ごとの文型バンクから決定論的に生成した 60 件を CC0 で置く。

**理由** 仕様書 31 節 4 項が明示的にこれを求めています。arXiv の Abstract は個々の投稿のライセンスに従い、一括で再配布可能とは限りません。公開リポジトリに置く以上、確認できないものは置かない、が唯一安全な選択です。

**品質のための工夫** 単なるプレースホルダーではなく、分野ごとに Background → Problem → Method → Result → Significance の 5 文で構成し、インライン LaTeX、DOI/arXiv 識別子、掲載誌、英語難易度、数式密度を持たせています。重複排除を試すための変種（DOI 表記ゆれ / arXiv v1・v2 / プレプリントと出版版）も 3 組含みます。

**Phase 1 以降** 実データは API から取得し、このコーパスはオフライン動作とテストのフォールバックとして残します。

---

## D-006 pgvector 列は Phase 3 まで作らない

**決定** `embeddings.vector_json` に JSON として保存し、`vector(N)` 列と ANN インデックスは Phase 3 のマイグレーションで足す。ただし docker-compose と CI は最初から `pgvector/pgvector:pg16` を使う。

**理由** Phase 0 にベクトル検索は 1 つもありません。次元数は使う埋め込みモデルで決まり、まだ決めていない値をスキーマに焼き込むと、決めた瞬間にマイグレーションのやり直しになります。一方で、環境側に拡張が無いせいで Phase 3 が止まるのは避けたいので、コンテナイメージだけ先に揃えてあります。

**`model` と `version` を Phase 0 から一意キーに含めている理由** 再埋め込み時に、異なるベクトル空間の値が同じテーブルに混在すると、距離計算が静かに壊れます。あとから足せる制約ではないので最初から入れています。

---

## D-007 ゲスト認証のみ、トークンはメモリ内

**決定** Phase 0 は `POST /auth/guest` だけを実装し、HS256 の JWT を返す。モバイル側はメモリにのみ保持する。

**理由** 仕様書 4 節のオンボーディングはサインアップを要求しません。最初のカードに辿り着くまでにアカウント作成を挟まないことが体験の前提なので、ゲストが一級市民である必要があります。メール認証は、それを壊さない形であとから足せます。

**Phase 1 で変えること** トークンを `expo-secure-store` に移します。アクセサの形（`getToken()`）は変えないので、呼び出し側は影響を受けません。

**安全側の既定** `PAPERMATCH_ENVIRONMENT` が `development` / `test` 以外のとき、開発用の既定秘密鍵のままだとアプリは起動を拒否します。

---

## D-008 ライセンス不明のレコードは保存しない

**決定** `abstract_redistributable` が false のレコードは取り込み時にスキップし、件数を監査ログに記録する。

**理由** Abstract を削って保存する案も考えましたが、Abstract の無いカードは Discover で表示できず、行が残っていればいつか誰かが「せっかくあるから」と使ってしまいます。仕様書 21 節の趣旨は「不明なものは扱わない」なので、行ごと作らないほうが意図に忠実です。

**代償** 候補が減ります。fixture では 60 件中 11 件がこれに該当し、パイプラインがこの分岐を必ず通るようにしてあります。

**見直す条件** メタデータのみのカード（タイトル・著者・原文リンクだけを見せ、Abstract は出典サイトで読ませる）を導入すると決めたとき。

---

## D-009 数式の途中で切れた選択は Abstract 全体を見て判定する

**決定** `selection_splits_math(full_text, start, end)` は選択範囲ではなく Abstract 全体を受け取る。

**理由** 実装中に判明した点です。ユーザーがある数式の途中から次の数式の途中までドラッグすると、選択範囲は `"$ ... where $"` という形になり、**構文的には完全に正しいインライン数式**として mask されてしまいます。選択範囲だけを見ている限りこれは検出できません。全体を mask して境界が数式スパンの内側にあるかを見れば確実に判定できます。

**結果** `POST /translations` はこの場合 422 `selection_splits_formula` を返します。壊れた LaTeX を含む翻訳を出すよりも、選択し直してもらうほうが良いと判断しました。

---

## D-010 翻訳が数式を壊したら原文を返す

**決定** Provider の出力にプレースホルダーの欠落があれば、翻訳ではなく原文を返し、`fellBackToOriginal: true` を立てる。

**理由** 数式が 1 つ抜けた翻訳は、抜けたことがユーザーから見えません。「訳せませんでした」と分かるほうが、静かに間違った内容を読ませるより安全です。仕様書 3 節の Trust by provenance と 7 節の数式保護の当然の帰結です。

**例外** 段階ヒントの `hard_words` は難語の一覧であって文の再現ではないため、数式を含まないのが正常です。この段階だけ欠落検査を行いません。

---

## D-011 タイトル正規化は `&` を `and` に寄せる

**決定** `normalize_title` は NFKC 正規化・ダッシュ統一・LaTeX 除去に加えて、`&` を ` and ` に置換する。

**理由** テスト作成中に見つかった実際の分岐です。同じ論文でも、Crossref と OpenAlex で `"Anomaly Inflow & Defects"` / `"Anomaly Inflow and Defects"` のように割れることがあります。正規化しないと、DOI を持たないレコード同士が別物として残ります。

---

## D-012 UI 文字列は Phase 0 から i18n 化する

**決定** 日本語 UI しか出さない段階でも、可視文字列をコンポーネント内に直接書かない。

**理由** 仕様書 25 節が明示しています。あとから i18n 化すると、全ファイルを走査して文字列を探す作業になり、必ず取りこぼします。最初から辞書経由にしておけば追加コストはほぼゼロです。英語辞書も同時に用意し、キーの欠落はテストで検出します。

---

## D-013 TypeScript のテストランナーは用途別に分ける

**決定** `packages/*` は Node 組み込みの `node --test`、`apps/mobile` は `jest-expo`。

**理由** 共有パッケージは React にも React Native にも依存しない純粋な TypeScript なので、jest を持ち込む理由がありません。逆にモバイルは React Native のモックが要るため `jest-expo` が必要です。それぞれに合ったものを使い、ルートの `npm test` が両方を呼びます。

**注記** `jest-expo@57` は jest 29 系を前提としています（jest 30 を入れると `jest-runtime` と `jest-mock` のバージョンがずれて全スイートが起動時に落ちます）。`apps/mobile` の jest は 29 に固定しています。

---

## D-014 Expo の依存はバージョンを直接指定する

**決定** `npx expo install` ではなく `npm install` で SDK 57 対応バージョンを明示的に指定する。

**理由** この開発環境では `expo install` が Expo の互換性 API に到達できません（プロキシが 403 を返す）。SDK 57 に対応するバージョンを `package.json` に固定してあるので、インストールは `npm ci` だけで再現します。

**注意点** SDK を上げるときは、ネットワークが通る環境で `npx expo install --fix` を実行し、その結果を commit してください。

---

## D-015 `index.html` は残す

**決定** リポジトリに元からあった無関係な `index.html`（参考文献リンク集）を削除しない。

**理由** PaperMatch とは関係のないファイルですが、削除を依頼されていません。不要であれば削除は 1 コマンドです。

---

## D-016 実データ Provider（arXiv / OpenAlex）は Phase 1 で未接続

**決定** Phase 1-A（arXiv / OpenAlex Provider）を実装せず、Phase 1-B と 1-C を先に完成させた。

**理由** この開発環境のネットワークポリシーがパッケージレジストリ以外への外向き接続を遮断しています（`export.arxiv.org` と `api.openalex.org` はプロキシが 403 CONNECT を返します）。Provider を書くこと自体はできますが、実際のレスポンスに対して一度も動かせないコードを「完了」として commit するのは誠実ではありません。Atom XML と OpenAlex JSON の記録済みレスポンスに対するオフラインテストは可能なので、Phase 1-A はその形（記録レスポンス + 手動での疎通確認手順）で次のスライスとして実装します。

**現状の影響** フィードは合成コーパス 49 件で動作します。仕様書 29 節の「100件以上の実データ候補」は未達で、これは Phase 1-A の完了条件のままです。

**追記（Phase 1-A 実装後）** Provider 本体は記録形状の fixture に対して完成しました（D-025）。実 API への疎通だけが `-m live` の opt-in テストとして残っています。

なお、遮断そのものが 1 件バグを見つけました。プロキシが返す 403（HTML 本文）を、パーサが「unparseable XML」として報告していました。`HttpProviderClient` は 4xx をあえて raise せずに返す設計（こちら側のクエリ不備でブレーカーを開かないため）なので、**ステータス確認は Provider 側の責任**です。これを怠ると「arXiv がリクエストを拒否した」が「XML が壊れている」として報告され、次に調べる人がパーサを開くことになります。両 Provider に `_fetch` を追加してステータスを先に見るようにしました。

---

## D-017 フィードの 70/20/10 は「枠の配分」であり、スコアの重みではない

**決定** 候補を「選択分野 / 隣接分野 / 未探索」の 3 プールに分け、7:2:1 のブロックを繰り返して並べる。

**理由** 仕様書 16 節の 70/20/10 を score の重み係数として実装すると、比率はどこにも保証されません。枠として実装すれば比率が構造的に保たれ、しかも各カードの推薦理由が本当のことになります（隣接プールのカードは「隣接だから」そこにいる）。

**実装中に判明したこと** 多様性の並べ替えを別パスとして後段に置くと、この比率が崩れます。隣接カードを後ろに送ると選択分野のカードが前に繰り上がり、7/2/1 のはずのページが 9/1/0 になりました。そのため「枠がプールを決め、その中で多様性が候補を決める」1 パスに統合しています。

**もう 1 点** プール判定に分野の重みしきい値（0.15）が必要でした。閾値なしだと、重み 0.1 の付随タグだけで機械学習の論文が「高エネルギー理論の選択分野」に入り、比率が事実上崩壊します。

---

## D-018 範囲選択は文単位

**決定** 任意のドラッグではなく、文をタップして選択し、隣の文をタップすると範囲が伸びる方式。

**理由** React Native の `selectable` テキストはプラットフォーム標準の選択 UI を出しますが、**選択されたオフセットを JS 側から読む手段がありません**。API は正確なオフセットを必要とします（翻訳のアンカー、そして数式の途中で切れた選択の検出のため）。

文単位は仕様書 7 節の理想（任意範囲）より粗いですが、「分からない箇所だけ選択」は実際にはほぼ文単位ですし、ネイティブの不可視な選択にはない性質が 2 つあります: **各文が個別にスクリーンリーダーから到達可能**であること、そして **API に渡すオフセットが厳密**であること。

**Phase 2** 語単位の選択にはネイティブモジュールか WebView が必要です。文単位の選択オフセットは同じ API で通るので、置き換えは局所的です。

---

## D-019 Undo は「効果」と「痕跡」の両方を戻す

**決定** Undo は saved 行を消すだけでなく、その論文の impression も削除する。

**理由** フィードは表示済みの論文を除外します。impression を残したまま save 行だけ消すと、ライブラリからは消えているのに Discover には二度と現れない、という状態になります。ユーザーから見れば「取り消したのに戻ってこない」であり、仕様書 29 節の「Undoが機能する」を満たしません。

action ログは削除せず `undone` を立てて補償 action を追加するので、表示→スキップ→取り消し の履歴は分析用に残ります。

---

## D-020 action に単調増加の sequence 列を追加

**決定** `actions.sequence`（BIGSERIAL、migration 0002）を追加し、「直前の action」はこの列で決める。

**理由** `created_at` は `now()` を使っており、PostgreSQL ではこれは**トランザクション開始時刻**です。1 リクエスト内で 2 つの action を書くと同じタイムスタンプになり、順序が UUID のランダムな大小で決まってしまいます。Undo は何を戻すのか正確に知る必要があるので、これは許容できません。

---

## D-021 実行して初めて見つかった 4 件

いずれもテストが通っていた状態で、実際にアプリを起動して初めて判明したものです。記録として残します。

1. **`fetch` の receiver** — `this.fetchImpl = options.fetchImpl ?? fetch` としてプロパティに素で置き、`this.fetchImpl(...)` で呼ぶと、ブラウザは `this` がグローバルでない `fetch` 呼び出しを "Illegal invocation" で拒否します。この TypeError はリクエストの try 内で投げられるため、**全 API 呼び出しがユーザーには「オフライン」として見えていました**。回帰テストを追加済みです。

2. **`aria-hidden` の欠落** — 背面カードには `accessibilityElementsHidden` と `importantForAccessibility` を設定していましたが、react-native-web はどちらも `aria-hidden` に変換しません。ブラウザのアクセシビリティツリーに論文が 2 本入っていました（スクリーンリーダーが同じ画面で 2 本読み上げる状態）。3 つ目のプロパティとして `aria-hidden` を追加しています。

3. **Pan ジェスチャーがタップを飲む** — カード全体が Pan の対象なので、文のタップがジェスチャーに吸収されて選択が動きませんでした。`activeOffsetX/Y` を 12px に設定し、実際に動くまで Pan が起動しないようにしています（タップの取りこぼしが直るだけでなく、選択のつもりでカードが揺れるのも止まります）。

4. **Abstract が 1 文 1 行で切れていた（Phase 1-D で発見）** — 文ごとのタップ領域を作るために、各文を `flexDirection: 'row'` + `flexWrap: 'wrap'` の中の `Pressable` として並べていました。flex の子は自分の中で折り返さないので、**各文が折り返さない 1 行になり、カード幅で切れて 40 文字程度しか読めない**状態でした。仕様書 6 節でカードの本体は Abstract なので、これは表示の乱れではなく機能の欠落です。入れ子の `<Text>` に変更しました — 文ごとの `onPress` とアクセシビリティラベルを保ったまま、通常の行送りで折り返す唯一の構造です。**コンポーネントテストは文字列の存在しか見ないので、この種の不具合は通過します。**

**訂正（Phase 1-D）** ここには以前「3 の修正後も web ビルドでは文の選択が動かない」と書いていましたが、**これは誤りでした**。ブラウザで実際に確認したところ、文タップは選択ハイライトを付け、翻訳シートも開きます。当時そう判断したのは、シートが開いたかどうかを間違った文字列で探していたためです。**実機での確認は依然として必要です** — マウスのクリックはタッチのジェスチャーではなく、Pan と tap の競合が実際に起きるのはタッチ入力のほうなので、TASKS.md のチェック項目は残します。

---

## D-022 構造分類は `ai` ではなく `heuristic` と名乗る

**決定** キューフレーズによる規則ベースの Abstract 構造分類を実装し、その出力を `detected_by='heuristic'` として保存する。語彙 `detectionMethod` に `heuristic` を追加した（migration 0003）。

**理由** 仕様書 8 節は「AI分類の場合は自動検出ラベルを付け、原文を変更しない」と書いています。ラベルの要点は *どうやって付けたか* を読者に伝えることなので、キューフレーズ照合を `ai` と名乗らせると、そのラベルが持つ以上の信頼を主張することになります。仕様書 3 節の Trust by provenance に照らして、これは書き換えではなく名前の問題です。

**なぜ規則ベースを先に入れたか** カードの役割ラベル表示にも、Phase 2 の「Before you read」が method/result の文を見つけるのにも構造が要ります。学術 Abstract はジャンルとして定型性が高いのでキュー照合でもかなり当たり、コストはゼロで、オフラインで動き、そして何より **後からモデルを入れたときに比較対象になる**。fixture コーパスは `detected_by='source'` の正解ラベルを持っているので、この比較はテストとして常時回っています。

**実測** 60 件のコーパスに対して平均一致率 0.89、完全一致 34 件。テストは 0.80 / 25 件を下限にしてあります（キューの調整では落ちないが、実際の退行では落ちる水準）。

---

## D-023 復習は 2 択・固定間隔で、点数を付けない

**決定** 復習の答えは `again` / `got_it` の 2 つだけ。間隔は 3→7→16→35→70 日の固定はしごで、SM-2 のような ease factor は持たない。`again` は 0 に戻さず 1 段だけ下げる。

**理由** 仕様書 9 節と 10 節が繰り返し言っているのは「評価や罰ではなく」「派手な点数化はせず」です。SRS の一般的な設計（ease factor、連続日数、正答率）はどれも点数化で、しかも 1 日空けただけで失うものがある設計になりがちです。仕様書が求めているのは「数日後に表現を1件提示」であって、こなすべきキューではありません。

**`again` を 0 に戻さない理由** ほぼ覚えていたものを 1 回外しただけで過去の復習が全部消えるのは、まさに仕様書が排している「罰」です。1 段戻すだけなら、覚え直しのコストは払いつつ積み上げは残ります。

**キューの既定件数は 5** 総数は正直に返しますが、既定で見せるのは一握りです。

---

## D-024 Phase 2 のうち AI が要る部分は着手しない

**決定** 実翻訳 Provider、Why it matters、Before you read の AI 生成部分は Phase 2 から外し、AI なしで完成できる部分（構造分類・表現辞書・復習・保存理由）だけを実装した。

**理由** D-016 と同じ理由です。この環境は外向きのネットワークが遮断されており、LLM API の鍵もありません。プロンプトと Provider 実装を書くことはできますが、一度も実際の応答に当てられないものを「完了」として commit すると、動いていないコードが完了扱いで積み上がります。

**Provider interface は Phase 0 から存在します**（`ExplanationProvider`）。接続時に触るのは registry への登録 1 行と実装ファイル 1 つで、呼び出し側は変わりません。

---

## D-025 arXiv と OpenAlex のライセンス上の立場

**決定** arXiv API が返すメタデータ（タイトル・著者・Abstract・カテゴリ）は **CC0 1.0** として扱い、`license_id="CC0-1.0"` と `sourceTermsUrl=https://info.arxiv.org/help/api/tou.html` を全レコードに付けて保存する。OpenAlex は **データセット自体が CC0** であることを保存の根拠にしつつ、読者に示すライセンスは **論文ごとの `best_oa_location.license` / `primary_location.license`** から取る。知らないライセンス文字列は推測せず `license_id=None` / `abstract_redistributable=False` とし、ingestion はそのレコードの本文を取り込まない。

**なぜ 2 つのソースで扱いが違うのか** arXiv API のライセンスはメタデータ全体に一律で掛かるもので、論文ごとの投稿ライセンス（arXiv perpetual license, CC BY, CC BY-NC-SA など）とは別のレイヤーです。API から取れる範囲＝メタデータしか扱わない以上、判断すべきなのは前者だけです。OpenAlex は逆に、集約元の OA ロケーションごとのライセンスをそのまま持っているので、論文単位で答えられます。同じ「CC0」でも根拠が違うので、`license_id`（読者に示す値）と `raw.datasetLicense`（保存の根拠）を別のフィールドに分けています。

**知らないライセンスを推測しない** `KNOWN_LICENSES` にない文字列（fixture では `publisher-specific-oa`）は「たぶん再配布できる」とは扱いません。仕様書 21 節の「ライセンス不明の本文断片を扱わない」はここが実装箇所で、`abstract_redistributable=False` が ingestion のスキップ条件になります。ライセンス文字列そのものを保存して人間が後で判断する、という選択肢もありますが、判断されるまでの間そのレコードが「ライセンス欄が埋まっている」状態で存在するほうが危ういと考えました。

**やっていないこと 2 つ**

1. **出版社サイトのスクレイピングはしません。** 仕様書 21 節が明示的に禁じており、Provider は公式 API しか叩きません（`API_URL` は 2 つとも定数で、リダイレクト追従もしていません）。
2. **商用利用に向けた個別の利用規約レビューは未実施です。** 仕様書 21 節は「商用利用時は各ソースの規約を個別に確認」と書いていますが、これは法務判断であってコードで代行できるものではありません。**このリポジトリは研究・個人利用の前提で書かれており、商用配布の前に arXiv・OpenAlex 双方の規約を人間が確認する必要があります。** ここに書いているのは「確認したので安全です」ではなく「確認していないので、する必要があります」です。

**検証可能にしてある理由** ライセンスの主張は根拠 URL なしでは検証できないので、`sourceTermsUrl` をレコードに同梱しています（`test_the_licence_position_is_recorded_and_checkable`）。規約が変われば URL の先が変わるので、少なくとも「どこを見れば分かるか」は残ります。

---

## D-026 TypeScript にも lint と formatter を入れ、React Compiler の指摘は原則として直す

**決定** ESLint 9（flat config・`eslint-config-expo` 57 系）と Prettier 3 を導入し、`make lint` と CI の必須チェックに加えた。`eslint-config-prettier` を最後に置き、整形は Prettier だけが担当する。

**プロジェクト固有のルールを 2 つ足しています**

- `no-restricted-syntax` で **スタイル中の 16 進カラーリテラルを禁止**。仕様書 19 節はパレットを `@papermatch/design-tokens` の 1 箇所に置くと決めており、20 節の色覚に依存しない対比はそこが単一の出所であることに依存しています。実際に 1 件見つかりました（`AbstractCard` の `shadowColor: '#000'`）。ダークテーマでは黒い影はカード面に対して見えず、**「後ろにもう 1 枚ある」という唯一の手がかりが消えていました**。`color.shadow` トークンを両テーマに追加して置き換えています。
- `no-restricted-imports` で **画面どうしの相互 import を禁止**。画面はエントリポイントであって再利用単位ではないので、共有したくなったら `src/` に出す、というルールです。

**React Compiler 由来のルールで 13 件のエラーが出ました。うち 4 件は実バグでした。**

1. **`profile.tsx` の `Section` が render の中で定義されていた** — render のたびに別のコンポーネント型になるので、React は部分木を毎回 unmount して mount し直します。設定チップを 1 つ切り替えるたびにフォーカスが飛ぶ、つまり**スクリーンリーダー利用者はトグルのたびにセクション先頭へ戻される**状態でした。モジュールスコープへ出しています。
2. **`index.tsx` の選択範囲がカードをまたいで残りうる** — 選択は文のインデックスで持っており、`rangeToSelection` はそれをカード本文の文字オフセットへ変換します。カード切り替え時に effect で消していましたが、消えるまでの 1 render の間、**前の論文のインデックスが今の論文の本文に適用されます**。選択に paperId を持たせ、一致しなければ選択なしと見なす形に変えました（`selectionFor`、回帰テストつき）。effect で後から消すのではなく、そもそも成立しない構造にしています。
3. **`session.tsx` が `useMemo` の返り値を mutate していた** — token を保持するオブジェクトを `useMemo` で作って書き換えていました。React が memo を破棄すると値が黙って巻き戻ります。`useRef` に変更し、あわせて **どこからも読まれていない `token` state を削除**しました（読み手は `status` と `user` だけで、この state は再レンダリングを起こすだけでした）。
4. **`useDeck.ts` が render 中に ref を書いていた** — 破棄されうる render が書いた値を、非同期コールバックが「今のデッキ」として読む可能性がありました。effect での更新に変更しています。

**残した指摘 4 件と、その理由**

`react-hooks/set-state-in-effect` の 3 件（`saved` / `learn` / 翻訳シートのデータ取得）は、**ルールのほうが正しい**が、正しい直し方はデータ取得層の入れ替えです。これらは「mount 時に取得し、途中経過を state で持つ」という形で、React が勧めているのはまさにそれをやめて `@tanstack/react-query`（依存には既に入っています）に任せることです。オフライン時のキャッシュ・フォールバックが絡むので、lint 導入と同じ commit でやる作業ではありません。**行ごとの disable と理由コメントを置き、TASKS.md に移行項目として立てました。**

`react-hooks/refs` の 1 件（`session.tsx`）は**誤検出です**。ref を読んでいるのは `getToken` の中で、これはクライアントがリクエストを組み立てるときに呼ばれます。ルールはクロージャが *生成される* 場所しか見ないので render 中の参照に見えています。

**翻訳シートは閉じたときに unmount するようにしました** — 以前は mount したまま effect で state を初期化していました。unmount は同じ初期化を「間」なしで行うので、**別の選択でシートを開き直したときに、新しいリクエストが返るまでの数フレーム前の訳が出る**ことがなくなります。

**Prettier の対象から外したもの** Markdown と、手で表形式に整えた JSON（`tokens.json`、`fixtures/`、`generate-fixtures.mjs`）です。Prettier は表の桁を**文字数**で揃えますが、日本語の文字は等幅表示で 2 桁分あるので、揃えたはずの表がかえって崩れます。トークン表も 1 行 1 概念で light/dark を横に読む形が要点で、1 行 1 キーに展開されると比較ができなくなります。いずれもテストで担保しています。

---

## D-027 LaTeX は書き換えず、通すか拒むかだけを決める

**決定** `text/latex_safety.py` は **バリデータであってサニタイザではない**。危険な要素を取り除いた式を返すことはせず、その式をレンダラに渡してよいかどうかと、駄目な理由だけを返す。拒否された式も API のレスポンスには**そのまま乗る**。

**理由** 仕様書 11 節は「原式を勝手に変換しない」と書いています。一部を削った式は、**論文の式に見えるが論文の式ではないもの**で、原文を主役に置くと決めたアプリにとってこれ以上悪い出力はありません。そして 11 節は失敗時の挙動も決めています——「整形済み LaTeX ソースと原文リンク」。つまり拒否は「表示しない」ではなく「組版せずソースとして見せる」なので、文字列自体は送る必要があります。

**何を拒むか、なぜそれが危ないか** 式は WebView の中の KaTeX に渡ります。WebView はブラウザなので、式文字列はレンダリングエンジンに届く未検証入力です。

| 分類 | 例 | 危険の中身 |
| --- | --- | --- |
| 文書外への到達 | `\href` `\url` `\html*` `\includegraphics` | `javascript:` / `data:` URL はそのまま WebView 内でのスクリプト実行。`\html*` はクラス名・id・インラインスタイルをホスト文書に注入します |
| マクロ定義 | `\def` `\newcommand` `\csname` `\expandafter` | 展開爆弾（billion laughs）の書き方そのもの。KaTeX の `maxExpand` に頼るのは順序が逆で、拒否できる入力をレンダラ側の上限で受け止めることになります |
| ファイル・エンジン操作 | `\input` `\write` `\catcode` | KaTeX には無意味ですが実際の TeX エンジンには意味があり、このテキストはいつかそちらに渡るかもしれません |
| レイアウト爆弾 | `\rule{1pt}{99999em}` | 画面が固まる高さの箱。**コマンドではなく寸法**を上限にしています（`\rule` 自体は正当な組版です） |

**判定はサーバで行います。** 仕様書 25 節に従い、「この文字列を自分のレンダラに渡してよいか」をクライアントに判断させません。クライアントが 1 回のリリースで間違えれば終わりです。API は `renderable` と `refusalReasons` を返し、クライアントはそれに従います。

**意図的に安全側へ倒している点** `a \\href` は本来「改行 + href という文字列」ですが、2 つ目のバックスラッシュを `\href` の開始として読み、式を拒否します。誤検知の代償は式 1 本がソース表示になることで、見逃しの代償は WebView でのスクリプト実行です。通常の書き方（`\\` の後に空白）は影響を受けません。

---

## D-028 「検証済み」を fixture に書かせない

**決定** 手動作成の数式カード（`fixtures/math-cards.json`）は **`verificationStatus` を一切持たない**。持つのは*チェックそのもの*——数値的に標本化する恒等式と、次元の釣り合い——で、状態は取り込み時に `papermatch_api.mathcheck` を実際に走らせた結果を保存する。

**理由** 仕様書 12 節は 6 つの検証状態を定め、「未検証の変形は既定で非表示」と決めています。ここで fixture に `"verificationStatus": "mechanically_verified"` と書けるようにしてしまうと、「検証済み」は**誰かがそう入力した**という意味にしかなりません。それは 12 節が避けようとしていることそのものです。テストで、`cards` 配下に検証状態の文字列が現れないことを確認しています。

**チェックの中身と、その限界** CAS はありません（この環境に sympy はなく、外部にも出られません）。やっているのは、**LaTeX と並べて著者が書いた機械可読な形**を標本評価することです。式の両辺を通常の式として書き、変数と範囲を宣言し、それを 25 点で比較します。誤った変形はほぼすべての点で食い違うので、これは実際に効くチェックです。ただし弱点が 2 つあり、どちらも隠さずに書いておきます。

1. **宣言した範囲でしか主張していません。** 範囲は主張の一部なので、evidence に記録して保存します。
2. **チェックが、隣にある LaTeX と食い違っていても検出できません。** これは LaTeX を解析しない限り閉じない穴です。それでも得られるのは、「検証済み」と書かれた step には*何か機械的なことが実際に起きた*という保証です。

**空虚なチェックへの対策** 変異テスト（片辺を摂動して失敗するか）だけでは不十分でした。トートロジー（`A` と `A` を並べ替えただけ）は摂動すれば壊れるので変異テストを通ってしまいます。**実際、最初に書いた 2 件がそれでした。** そのため両辺が空白除去後に同一なら落とすテストを追加し、該当した step は書き直しました。

- ガウス積分の 1 段目は「2 次元ガウシアンが 1 次元 2 つに分解する」という*点ごとの恒等式*に置き換え
- 2 段目は極座標代入そのもの（`x = r cosθ, y = r sinθ` を入れて `r` だけの関数になること）を標本化
- 3 段目は原始関数を差分商で微分し、被積分関数と一致するかを確認

**チェックできないものは、チェックしたことにしない。** フォン・ノイマン エントロピーの対角化 step の中身は「トレースが基底に依らない」という線形代数の事実で、標本評価では確かめられません。ここにトートロジーを書けば `mechanically_verified` が取れてしまうので、**数値チェックを付けず次元チェックだけにして `dimensionally_checked` に留めています。**

**`combined_status` が返さないもの** `source_exact` と `human_reviewed` は「どこから来たか」「誰が見たか」の主張で、どれだけ計算しても得られません。関数の返り値からこの 2 つを除外し、テストで固定しています。このコーパスは誰もレビューしていないので、カードの `review_status` はすべて `draft` です。

---

## D-029 KaTeX はアプリに同梱し、数式は「データ」としてだけ WebView に渡す

**決定** KaTeX の JS・CSS・フォントをビルド時に 1 つのモジュール（`apps/mobile/src/math/katexRuntime.ts`、約 650KB）へインライン化し、数式は `<script type="application/json">` に入れて `textContent` から読む。**LaTeX を HTML へ文字列結合しない。**

**なぜ同梱か** CDN は 2 つの理由で使えません。仕様書 26 節はオフライン動作を求めており、電車の中で数式が組めないアプリは要件を満たしません。そして仕様書 25 節に照らすと、レンダリング時に外部から実行可能コードを取ってくること自体が避けたい依存です。フォントは woff2 のみを入れています（KaTeX は woff2 / woff / ttf を全て同梱しますが、WKWebView と Android System WebView はどちらも何年も前から woff2 に対応しており、3 形式抱えると容量が 3 倍になるだけです）。使わない形式の `@font-face` は**残さず削除**しています — 残すと、無いファイルを取りに行く静かなネットワークリクエストになります。

**なぜ「データとして」が絶対条件か** 数式は WebView の中の KaTeX に渡ります。WebView はブラウザなので、**マークアップとして届いた数式文字列はスクリプト実行**です。`JSON.stringify` だけでは足りません — `</script>` を含むペイロードは、引用符をどう escape してもそこでブロックを閉じます。ブラウザは JS エンジンに渡す前に `</script` を探すからです。そのため `<` `>` `&` を `\uXXXX` 形へ、さらに JSON では有効だが JavaScript では行終端子になる U+2028 / U+2029 も escape しています。

**同じものを 3 重に止めています。** この 1 コンポーネントだけが未検証の内容を実行するので、独立した層を重ねました。

1. **サーバ**が `\href` や `\html*` を拒否する（D-027）
2. **KaTeX の設定**が `trust: false` でそれらを無効化する（サーバが拒否していない前提で構成する）
3. **Content-Security-Policy** が `default-src 'none'` であらゆるロードを禁じ、**`onShouldStartLoadWithRequest`** が最初以外のナビゲーションを拒否する（仕様書 25 節）

**実ブラウザで確認したこと** 単体テストは文書の *文字列* を見るだけなので、生成した文書を Chromium に実際に読ませて確認しました。同梱コーパスの 18 本すべてが組版され全てに MathML があること、`</script><script>window.__alerted=1</script>` という数式では **script 要素が 3 つ（こちらの分だけ）で注入スクリプトは走らない**こと、壊れた式は LaTeX ソースにフォールバックし `{ok:false, error}` を post すること、Dynamic Type が効くこと、そして**ネットワークリクエストが 0 件**であること。

**MathJax へのフォールバックは実装していません。**（→ **D-062 で撤回。実装済みです。**）仕様書 11 節は KaTeX → MathJax → ソース表示という 3 段を挙げていますが、MathJax の同梱はさらに約 1MB で、KaTeX が扱えない構文の大半はサーバ側で既に弾いています。そして**信頼の観点で効いているのは 3 段目**（ソースを見せる）です — 読者が原文と突き合わせられる状態を保つことが要点で、そこは実装済みです。MathJax は TASKS.md に残しました。

> 追記（D-062）: この段落は費用の話であって、できない理由ではありませんでした。実測すると 5 種類の構文が KaTeX 不可・MathJax 可で、読者に見えるのは等幅のソース文字列が組版された式になる差です。同梱費用も「約 1MB」ではなく 2059KB でした。

**未確認の点** WebView コンポーネント（`MathView.tsx`）自体は**この環境で実行できていません**。react-native-webview は web ビルドで動かず、実機もありません。文書生成側は純関数として全てテストし実ブラウザで描画確認していますが、**高さの通知・ナビゲーション拒否・アクセシビリティは実機確認が必要**で、TASKS.md のチェックリストに入れています。

---

## D-030 依存バージョンは Expo Go が持つネイティブに合わせる。ただし JS は別

**きっかけ** 実機（Android / Expo Go）でアプリがスプラッシュから進まず、「PaperMatch」の文字だけの白画面で止まりました。バンドルは成功しており、赤いエラー画面も出ません。

**原因** `react-native-gesture-handler` を `~3.1.0` で入れていましたが、**Expo Go SDK 57 が内蔵しているネイティブは 2.32** です。`GestureHandlerRootView` はアプリ全体を包んでいるので、ここが噛み合わないと何も描画されません。さらに悪いことに、当時のツリーには **2.32 と 3.1 の両方**が入っていました（元の指定が hoist されて残っていた）。gesture-handler が 2 つあるのは、それ単体で壊れる状態です。

これは D-014 の付けです。`npx expo install` が proxy で 403 になるため SDK 57 のバージョンを手でピン留めしましたが、いくつか外しました。**正解は最初から `node_modules/expo/bundledNativeModules.json` にあり**、ネットワーク無しで読めました。今後バージョンを決めるときは、まずこのファイルを見ます。

**合わせるもの / 合わせないもの** ネイティブ側を持つパッケージ（`react-native-*`、`expo-*`、`@react-native-*`）は Expo Go が持っているものと一致させます。一方 **`react` と `react-dom` は JavaScript** で、バンドルしたものがそのまま動くので、Expo Go と一致させる必要はありません。ここも一度合わせに行って、`react-test-renderer` との組み合わせでテストが 23 件落ちました（`Invalid hook call`）。**「Expo Go に合わせる」はネイティブに対する要求であって、JS ライブラリの話ではありません。**

**併せて見つかった無限ループ** バージョンを揃えてアプリが起動するようになった直後、`Maximum update depth exceeded` が出ました。原因は 2 つ重なっていました。

1. `useDeck` が**毎レンダリングで新しいコントローラオブジェクト**を返しており、Discover 画面の effect が `[current, deck]` に依存していた。つまり effect が毎レンダリング走る。
2. その effect の中身は `noteImpression` の dispatch で、`impression_recorded` は**同じカードの重複でも必ず新しい state を返していた**。

結果として dispatch → 再レンダリング → effect → dispatch のループになります。1 は `useMemo` と、effect の依存を「コントローラ全体」から「記録関数」に絞ることで、2 は**何も変わらないときは受け取った state をそのまま返す**ことで直しました。2 のほうが本質的です — **変化のないアクションに対して等価な新オブジェクトを返す reducer は、state を見ている effect をすべてループの種にします。** 回帰テストは同一性（`toBe`）で書いています。

---

## D-031 詳細度スライダーは「説明の量」ではなく「導出の量」を選ぶ

**決定** Focus Mode の 最短 / 標準 / 一行ずつ / 初学者向け は、**カードが持っている導出のうちどれだけを見せるか**を選ぶもので、説明を生成し分けるものではない。どのレベルも、保存されていない step を作り出さない。

**理由** 仕様書 10 節のこの 4 段を「説明の詳しさ 4 段階」と読むこともできますが、そう作ると **最も詳しいレベルは必ず何かを捏造することになります** — カードには step 単位より細かいものが存在しないからです。実際に段階で変わるのは (1) 個々の操作を出すかどうか、(2) 「なぜ?」を最初から開くかどうか、の 2 つだけです。テストで「どのレベルでも、表示される step は保存済み step の部分集合である」ことを固定しています。

**最短の形を作り直しました。** 最初の実装は step を間引いただけで、式は 4 本すべて残り矢印だけが消えました。**途中に説明のない穴が空き、レンダリングの不具合にしか見えません。** いまは両端の式だけを出し、`途中の 3 手を省略` と件数を明記します。省略していることを言うのが、同じ意図の正直な形です。

**理解チェックはカードから作り、生成しません。** 仕様書 10 節の 次の一手 は、保存済みの導出だけで答えを作れます。誤答の選択肢は**同じカードの他の操作**です — 無関係な導出から持ってくると語彙だけで弾けてしまい、何も試していないことになります。同じカード内なら全部もっともらしいので、「ここで効くのはどれか」を分かっている必要があります。結果は文であって点数ではありません（10 節: 派手な点数化はせず）。返り値に `correct` と `messageKey` 以外を持たせないことをテストで固定しています。

**極限タブは実際に走ったチェックだけを報告します。** step の evidence に入っている「標本化した範囲」と「次元の釣り合い」をそのまま出し、`ここに書かれていない範囲については何も主張していません` と添えます。evidence を持たない step については何も言いません（未検証の変形に適用範囲を書けば、誰もやっていないチェックを説明することになります）。

---

## D-032 探索ボーナスは置かない。興味の項は分野の重みで刻む

**決定** 仕様書 16 節がスコアの構成要素として挙げている **探索ボーナスを実装しません**。代わりに、興味一致の項を「選んだ強さ × その分野が論文に占める重み」の連続値にしました。

**探索ボーナスを置かない理由** D-017 で 70/20/10 は**枠の配分**だと決めています。候補は 3 つのプールに分けられ、**プールごとに独立に並べ替えられます**。したがって「どのプールにいるか」だけで決まる項は、比較相手全員に同じ値が乗るので、**1 枚のカードも動かせません**。重みとテストと説明文を持ち続けて、証明できる範囲で何もしない算術を抱えることになります。探索プールが実際に必要としているのは「いつも同じ数本が出てこないこと」で、これはプール**内**のタイブレークです。ページングの seed に依存するので `feed.py` 側に置き、`breakdown` には `tie_break` として別名で出します — スコアの構成要素ではないからです。

**興味の項を刻んだ理由** これは危うく落とすところでした。書き上げた `interest_match` は「選んだ分野なら 1.0 / 隣接なら 0.5 / それ以外 0」という**所属判定**でした。ところが枠配分の下では、**matched プールのカードは全員が定義上 1.0 になります**。つまりモデルで最も重い項が、実際の並び順に対して何も言わなくなる。差し替え前の `feed.py` のインラインの式は `interest.strength * max(0.2, weight)` を使っており、こちらは論文ごとに違う値でした。**素直に差し替えていたら、信号を減らしながら「スコアラーを導入した」と報告することになっていました。**

いまは 1 本の式です。宣言された強さ（隣接分野は 0.5 を継承）に、その分野が論文に占める重みを掛けます。重みは先に 0.15 でふるいます（付随的なタグは主題ではない）が、通ったものには 0.2 の下限を置きます — 一部が選んだ分野である論文は、やはり選んだ分野の論文だからです。

**level 0 の扱いは移植しました。** 仕様書 18 節の level 0 は 数式を表示しない で、これは重み付けで釣り合わせる好みではありません。数式の多い論文はこの項で 0 になります。ただし除外はしません — 数学系の分野と level 0 を同時に選んだ読者のデッキが空になります。テストは固定値（`< 0.6`）ではなく **level 3 のときの同じ論文と比べる**形に書き直しました。固定値のしきい値は、level 0 が意味を失うような重み変更のあとも通り続けます。

**推薦理由とスコアの内訳は別物です。** `explain()` が返すのは `interest` や `freshness` といった**項の名前**、カードに出る理由は `enums.json` の固定語彙（`matches_field` / `adjacent_field` / `similar_to_saved` / `recent` / `foundational`）で、2 言語の文になります。スコアラーは並び順だけを決め、理由のラベルはそのまま残しました。両者が交わらないことをテストで固定しています。

---

## D-033 仕様書 16 節のフィードバック 5 つのうち、3 つは語彙自体が無かった

**見つけたこと** 16 節はフィード調整として 5 つを挙げています。このうち `hide_topic` と `hide_author` は初期スキーマからありましたが、**類似論文を減らす / 実験系を増やす / 古典的論文を増やす には対応する action type が存在しませんでした**。ボタンを置いても送り先が無い状態です。

**決定 1: 設定列ではなく action にする。** 3 つとも `actions` に記録し、フィードは「取り消されていない・30 日以内」の件数だけを読みます。16 節が否定的フィードバックを「今回は見送る」と扱えと言っている以上、**Undo と時間経過の両方で戻る**必要があり、このスキーマはすでに両方を知っています（D-019）。設定列にすると、取り消しも失効も自前で書くことになります。副次的な利点として、読者は自分が押したものを活動履歴でそのまま見られます — フィードの形が変わったことから逆算する必要がありません。

**3 回で飽和させ、そこで止めます。** 1 回はヒント、3 回で最大。20 回押しても永久追放にはなりません。「嫌い」と決めつけないというのは、そういう意味だと読みました。

**決定 2: `less_similar` は加算ではなく倍率。** スコアラーがすでに計算した類似ペナルティに掛けます。直前に見たものと似ていない論文にはペナルティが 0 なので、**何も起きません**。一律に引くと、一部の重複のために全カードが罰を受けます。値が 0 の項は `breakdown` に載せません — `less_similar: -0.0` が全カードに付くのは「ここで効いている」と読めてしまい、嘘になります。

**決定 3: 「実験系」を判定する分類器を書きました（`services/method_kind.py`）。** arXiv も OpenAlex も理論/実験の区別を公開していないので、無ければこのボタンは押しても何も起きないものになります。D-022 と同じ立場で、**規則ベースだと名乗ります**（`ai` ではない）。

**棄権します。** 200 語の要旨で判断がつかない論文は多く、そこでの正直な出力は**ラベルを付けないこと**です。無理に選ばせれば、難しい半分を確信を持って間違え、フィードはその間違いに従って並べ替えます。どちらのラベルも持たない論文は「判定していない」であって「どちらでもない」ではありません。ナッジ側も**実験系と分かっているものを持ち上げる**だけで、他を押し下げません。

**手掛かりは「何をしたか」であって主題ではありません。** `quantum` は証拠になりません。`we measured` `our apparatus` はなります。ここを外すと、分野分類器が別の名前を名乗っているだけのものになります。

**自分のテストに 2 つ直されました。** (1) 導出と測定が両方書かれた要旨が `theoretical` に倒れました。原因は `perturbative` と `non-perturbative` を**両方**列挙していたことで、1 語が 1.8 点になっていました。手掛かりが互いの部分文字列にならないことをテストで固定しています。(2) `we collected` としか書いておらず、**現在形の要旨を全部取り逃していました**（`calibration` と `calibrated` でも同じ間違いをしていました）。語幹に直しました。

**分類器を fixture に合わせて調整はしていません。** 直したのは上の 2 つの実際の欠陥だけで、そのあと出た結果をそのまま報告します。49 本中、実験系 2・理論 5・レビュー 12・**判定なし 30**。棄権率が高いのは設計どおりです。

**決定 4: ナッジの Undo はカードを呼び戻しません。** `skip` や `save` の Undo は、消えたカードをデッキに戻す必要があります。しかし「実験系を増やす」は**配合の要求であってこの論文を返せという要求ではなく**、すでに通り過ぎたカードが復活するのは頼んでいない変更です。`hide_topic` / `hide_author` は従来どおり戻します（UI がカードを送るのと同時に投げるため、ジェスチャ全体の取り消しになる）。

**UI は 5 つを 1 枚のシートに置き、押しても閉じません。** 「この話題を減らして、古典を増やす」は 1 つの考えなので、1 タップごとに閉じると 2 往復になります。**効かない行は理由を書いて無効化します** — 著者名の無い論文で「この著者を表示しない」が成功を報告して何もしないのは、無効化より悪いです。文言は日英とも「嫌い」「興味なし」を含まないことをテストで固定しました。

**シートを実際にブラウザで開いて見つけたこと。** 著者の行は「K. Grabowski の論文を…」と**名前を書く**のに、話題の行は「この分野のカードを…」としか書いていませんでした。シートはカードをほぼ覆うので、**開いた時点で読者はどの分野を抑えようとしているのか見えません**。分野 id をカードのチップと同じ表記で行に書くよう直しました。テストで両方が名指しされることを固定しています。

**もう 1 つ、これは環境の話。** 最初のブラウザ確認では action の POST が 422 で落ちました。原因はコードではなく、**`enums.json` を編集する前に起動していた uvicorn が古い語彙を保持していた**ことです。語彙は import 時に読まれるので、`enums.json` を触ったらサーバを再起動する必要があります。ただしこの失敗は、**シートが黙って何もしないのではなく「送れませんでした」と文で報告する**ことを実際に証明しました — 設計どおりに動いた失敗です。

---

## D-034 「いつ取得したか」と「いつ確かめ直したか」は別の列

**決定** `papers` に `last_refreshed_at` を足し、再取得キューの並び順はこちらで決める。`acquired_at` は使わない。

**きっかけ** worker を書いて動かしたら、1 回まわしたあとも **47 件すべてが「古い」ままキューに残っていました**。2 回目のパスの仕事量が 1 回目とまったく同じで、初めて気づきました。

**原因** キューを `acquired_at` で並べていました。ところが `acquired_at` は**仕様書 21 節が定める出自**で、値は provider が寄こします。`upsert_record` はそれをそのまま書き戻すので、**取得時刻を返さない provider（fixture を読む mock がまさにそれ）では、何度取得し直しても値が動きません**。結果、キューはいつまでも最も古いバッチを再チェックし続け、それより新しい論文には永久に到達しない。しかも `checked=47 unchanged=47` という**健全に見える数字を出し続けます** — だから読んでいるだけでは見つからなかったし、他のテストでも捕まりませんでした。

**直した形** 「いつ取得したか」（出自・provider が言う）と「いつ確かめ直したか」（運用・こちらが書く）は**別の問いなので、別の列**にしました。キューは `coalesce(last_refreshed_at, acquired_at)` で並べます。**provider が解決できなかった論文にも `last_refreshed_at` を立てます** — でないと今度はその 1 件がキューの先頭に居座り、後ろ全部を止めます（同じデッドロックの裏返し）。

回帰テストは、直す前の実装に戻すと**実際に落ちること**を確認してあります。

**併せて固定したこと。** provider が返さなくなった論文を**撤回扱いにはしません**。`get_by_canonical_id` の `None` は「provider がくれなかった」であって、一時障害・識別子の移動・そもそも持っていない、のどれでもあり得ます。これを撤回と読むと、**上流の不調 1 回で読者のフィードが静かに空になります**。件数は数えて監査に残しますが、`retraction_status` には触りません。

---

## D-035 Abstract の数式は組版する。カードは 1 枚に 1 つの文書

**きっかけ** デモのスクリーンショットを見ていて指摘されました。Discover カードの本文に
`$\mathbb{P}(|f - \mathbb{E}f| > t) \le 2\exp(-ct^2/\|f\|_{\mathrm{Lip}}^2)$` が
**LaTeX ソースのまま**出ていました。

**規模** 調べたら、このコーパスの Abstract の **95%** がインライン数式を含んでいました（1 本あたり平均 2.4 か所）。つまり例外ではなく、**ほぼ全部のカードが壊れて見えていた**ということです。仕様書 11 節は「インライン」を明示的に要求しており、LaTeX ソースの表示は**失敗時のフォールバック**です。常時フォールバックの状態でした。

**文字への置き換えは選びませんでした。** 数式を Unicode に写すやり方も考えましたが、実際のデータを見ると **18%（137 か所中 25 か所）が `\frac` / `\sqrt` / `\sum` / `\int` / `\binom` を含み、2 次元の組版を必要とします**。組める式と組めない式が混在した表示は、一貫していないぶん今より分かりにくくなります。それに、この repo は式について「勝手に変換せず、できないなら拒む」で通してきました（D-027・D-028）。

**カード 1 枚につき文書 1 つ。** 式ごとにレンダラを置くのではありません。仕様書 7 節の部分翻訳は**文単位で選ぶ**ので（D-018）、文は 1 つのタップ対象のままでなければなりません。式ごとに分けると、文が「まとめて選べない破片」になり、さらにカード 1 枚に何個もレンダラが載ります。いまは文が `role="button"` の span 1 つで、タップすると index を post します。

**後ろのカードと、数式の無い Abstract にはレンダラを置きません。** 後ろのカードは縮小・減光された予告でタップもできないので、読めないものを組むために払うコストです。数式が 1 つも無い Abstract（約 20 本に 1 本）も同じ理由で素通しです。

**平文の経路は消していません。** 文書が高さを報告するまで平文が出て、報告が来なければそのまま残ります。**レンダラが失敗したときに読者の手元に残るのが「さっきまでの Abstract」であって「空のカード」ではない**、という状態にしたいからです。この画面は、失敗しても後ろに何もありません。

**web は iframe、しかも `srcdoc` ではなく blob URL。** `react-native-webview` は web で動かないので、そのままでは web ビルドで Abstract の位置に「does not support this platform」と出ます。**ブラウザで検証できる唯一の画面がそれでは困ります。** `srcdoc` は親のオリジンを継ぐため文書の meta CSP 下でインラインスクリプトが走らず、KaTeX が黙ってソース表示に落ちます（置き換えようとしていたバグそのもの）。blob URL なら文書が自分のオリジンを持ち、CSP が文書に適用されます。frame は sandbox するので、選択の反映は「関数を呼ぶ」ではなく postMessage です。

**実ブラウザで確認したこと** 式が組まれること、MathML が併記されること、文をタップすると翻訳シートが開くこと、選択が色と下線の両方で付くこと、**`\href{javascript:...}` が `javascript:` リンクを 1 つも作らないこと**、外部リクエストが 0 件であること。

**書体を間違えて 1 回作り直しました。** 最初 iframe に sans のスタックを直書きしたら、**カードの中で Abstract だけが違う書体**になり、レンダリングの不具合にしか見えませんでした。いまはトークンの `abstract` ロール（セリフの読み書体）をそのまま渡します。**式を `nowrap` にしたのも見て直したところ**で、折り返しを許すと式の途中で改行され、文末の句点が次の行頭に取り残されていました。

---

## D-036 CI と同じものを 1 コマンドで回す（`npm run ci`）

**きっかけ** push してから CI が落ちたことが 2 回続きました。どちらも**環境差ではなく、私がコマンドの一部しか実行していなかった**だけです。

1. `npm run lint` は流したが `npm run format:check` を流さなかった。CI の lint ステップは 2 つを続けて実行しており、落ちたのは後者でした。
2. `tsc` を**テストファイルを書く前**に流し、書いたあとに流し直さなかった。`noUncheckedIndexedAccess` が配列アクセス 2 箇所を弾きました。

どちらも手元で 1 分の作業です。**「知っているのにやらなかった」を意志で直すのは無理なので、順番ごと 1 つのコマンドにしました** — `scripts/ci-local.sh`（`npm run ci`）が `.github/workflows/ci.yml` と同じものを同じ順で回します。PostgreSQL が要る 2 つ（pytest と migration 適用）は `--fast` で外せます。

**ワークフローにしか書かれていないチェックは、push して初めて存在を知るチェックです。** ci.yml を変えたら、このスクリプトも変えます。

---

## D-037 保存理由チップは「保存のあと」に出す。既定は選択肢にしない

**決定** 右スワイプは**その場で保存を確定**し、理由チップはそのあとの Undo トーストに並べる。既定の `interesting`（気になる）は**チップとして出さない**。

**理由** 仕様書 9 節は「右スワイプの既定は『気になる』。**任意タグ**：…」と書いています。つまり保存は理由の入力を待ちません。先にシートを出して理由を聞くと、**1 つのジェスチャーが 1 つの判断に変わり**、カードをめくっている読者はめくるのをやめます。6 節も 保存理由チップを**任意表示** です。

既定をチップにしないのも同じ理由です。それはスワイプがすでに意味したことで、選択肢として並べると「何か選ぶまで保存は確定していない」と読めてしまいます。

**Undo と同じ面に置きました。** 「保存しました / 取り消せます / 理由も付けられます」は 1 つの出来事についての 1 つの面です。別々のシートにすると、2 つの一時的な UI が同じ場所を取り合います。

**サーバが確認するまでチップを出しません。** まだ受け付けられていない行に `PATCH /saved/{id}` を送ることになるからです。Undo が `actionId` を待つのと同じ条件で、判定も同じ 1 か所です。

**チップの一覧は共有語彙から既定を引いて作ります。** 手で書き写すと `enums.json` にタグを足したとき、**どこにでもあるのに読者が選べる画面にだけ無い**という状態になります。テストで「既定 + チップ = 語彙全体」を固定しました。

**`PATCH` は置き換えなので、常に完全な集合を送ります。** `toggleReason` が既定を必ず含めて返すのはこのためで、落とすとサーバ側でも落ち、読者が触っていない「気になる」フィルタから論文が消えます。

**楽観的に光らせて、失敗したら戻します。** タグは小さい主張なので往復を待つと壊れて見えます。ただし**サーバが拒んだチップが光ったまま**にはせず、元に戻して文で伝えます。

---

## D-038 読者の「報告」と、レビュアーの「判断」は別のテーブル

**決定** `content_reports` を新設する。既存の `review_events` は使わない。

**理由** `review_events` の語彙は `approved` / `rejected` / `needs_changes` で、これは**その判断を下す立場にある人**の語彙です。読者が持っているのは「これは正しくないと思う」であって、その先どうなるかを決める権限ではありません。同じテーブルに入れると、**匿名のゲストが `approved` を主張できる**ことになります。

**報告は `verification_status` を動かしません。** あの列は「どの機械的チェックが走って、何を返したか」の記録です（`mathcheck/`）。読者の意見はチェックではありません。動かせるようにすると、**検証済みしか入らないことだけが価値である列に、誰も検証していない主張が入ります** — しかも `numerically_spot_checked` が下がっているのを見た読者に、それをやったのがチェッカーなのか通りすがりなのか区別する手段がありません。

**カードも隠しません。** 1 人の報告は全員から取り上げる根拠になりません。それは判断であり、判断は `review_events` の側です。報告がするのは、**判断する人がそのカードを見つけられるようにすること**だけです。

**1 人 1 対象 1 理由につき 1 件。** 27 節の指標は**率**なので、同じ読者が 2 回叩いたら 2 人になってしまいます。2 回目は上書きで、行は増えません。ただし**補足が空の再送で、先に書かれた補足を消しません** — 最初の実装は無条件に上書きしていて、同じチップをもう一度押した瞬間に読者が書いた文が消えました。「言い足せる」と書いたコメントと逆のことをしていたので直しました。

**報告先は「カード」ではなく「対象」です。** `equations` と `derivation_steps` は `provenance_kind` を持つので、**論文由来のものとモデルが書いたもので報告数を分けられます**。27 節のガードレールは「AI 説明の問題報告率」で、総数だけでは意味を成しません — 分けて初めて「モデルの書いたもののほうが苦情が多いか」が言えます。

**文言が約束しないこと。** このビルドでは誰もキューを見ません。だから「確認します」とは書かず、**実際に起きたこと（記録した）だけ**を書きます。シートの冒頭に「報告しても、このカードの内容や検証状態は変わりません」と先に出すのも同じ理由です — 報告で直ったと思った読者は、確かめるのをやめます。

---

## D-039 算出できない指標は、落とさずに「算出できない」と出す

**決定** 27 節の指標を `services/metrics.py` に実装し、`papermatch metrics` で出す。**データが無くて計算できないものは、省略せず値なしで並べる。**

**理由** 12 個の数字が並んだレポートは完成して見えます。12 個の数字と「これは算出できない、足りないのはこれ」の 3 行が並んだレポートは、**どこが見えていないか**を教えてくれます。いま算出できないのは 3 つで、どれも省くと存在ごと忘れられます。

- **数式カード完了率** — カードを「完了した」と記録するイベントがそもそも無い。
- **Canvas から論文詳細への遷移率** — Canvas は Phase 6 で、測る対象の画面が無い。
- **クラッシュ率・ジェスチャー失敗率** — クライアント計測が無い。

**分母は全部書きます。** ここで作れる間違いのほとんどは分母の間違いだからです。「10 秒以上読まれた割合」を**全 impression**で割ると、**クライアントが dwell を報告し損ねたときに数字が下がります** — いちばん動いてほしくないときに動く。だから分母は「dwell が記録された impression」で、レポートにもそう書きます。同じ理由で、分母が 0 のときは `0.000` ではなく**値なし**にします。0.000 は「誰もやらない」と読めますが、実際は「まだ誰も機会を得ていない」です。

**セッションの定義は仕様書に無いので、こちらで決めました。** North Star は「セッション数」を数えますが、セッションの定義がなく、アプリはセッション id を記録していません。**30 分の無操作で区切る**ことにし、定数にコメントとして理由を書きました（電話 1 本は跨げて、朝と夜が 1 つにならない長さ）。

**「自力で読み」の読み方も決めました。** 「翻訳を使わずに」とは取りません。**選択範囲の翻訳はプロダクトが働いているのであって、読者が失敗しているのではない**からです。9 節は「翻訳なしで読んだ割合」を別の数字として持っているので、仕様書自身が両者を分けています。

**走らせて自分のガードレールのバグを見つけました。** 「AI 説明の問題報告率」を **derivation step だけ**で数えていて、報告が入っている DB に対して 0 を返しました。読者が「説明が誤っている」を押すと**カードに対して**報告が立ち、カードにも `provenance_kind` があります。**ガードレールとしては最悪の壊れ方**です — 静かで、しかも安全に見える方向に間違える。いまはカードと step の両方を数えます。回帰テストは、直す前に戻すと落ちることを確認済みです。

---

## D-040 react-query に移す。ただしオフラインのフォールバックは queryFn の**中**に置く

**きっかけ** `react-hooks/set-state-in-effect` を 5 箇所で行ごとに disable していました（D-026）。本来の直し方はデータ取得をライブラリに任せることで、`@tanstack/react-query` は最初から依存に入っていたのに、Provider すら立てていませんでした。

**フォールバックの位置がこの移行の全部です。** react-query のキャッシュは**メモリ**なので、アプリが落とされた瞬間に空になります — **それはまさに、電車でアプリを開く瞬間**です。仕様書 25 節（API 障害時もキャッシュ済みを表示）と 26 節（オフライン）を満たしているのは AsyncStorage のキャッシュのほうなので、そこは 1 行も動かさず、**queryFn の中**に残しました。react-query が面倒を見るのは「いつ取りに行くか」だけです。

**オフラインは第 2 の state ではなく、データの一部にしました。** 1 つの関数が `{ rows, offline }` を返すので、**旗と行が必ず同じ試行から来ます**。別々の state にすると 1 レンダリングだけ食い違い得て（新しい行の上に「オフライン」の帯が出る、あるいはその逆）、しかもそれは誰かが報告するまで見えません。

**4xx はオフラインではありません。** `NetworkError`（リクエストが届かなかった）のときだけキャッシュを出します。それ以外はサーバが返事をして「だめだ」と言ったということで、そこにキャッシュを出すと**本物のエラーが古い行の裏に隠れます**。

**リトライは切りました。** queryFn がすでにキャッシュへ差し替えているので、リトライしても同じキャッシュに再度差し替わるだけで、時間だけ使います。

**翻訳シートだけは query ではなく mutation です。** `POST /translations` は選択と翻訳を**保存する書き込み**で、キャッシュから読む対象ではありません。副次的な効果として effect の問題も消えます — `mutate` は呼び出しであって setState ではないので、マウント時に撃つのは「effect の中で state を書く」ではなくなります。

**確認の仕方を間違えかけました。** 最初、保存タブを開き直してオフライン帯が出ないのを見て「壊した」と思いました。実際は `staleTime` 30 秒のあいだ react-query が自分のメモリキャッシュを返していただけで、データは本当に新鮮でした。**本当に確かめるべきは「ネットワーク無しでの起動」**で、そちらは行も帯も正しく出ます。

---

## D-041 検索は「保存した論文」を探す。コーパスは探さない

**決定** `GET /search` は**その読者の保存ライブラリだけ**を対象にする。全論文を検索する箱は作らない。

**理由** 仕様書で検索に触れているのは 3 箇所で、どれも保存済みを指しています — 2 節の 5 番目の job「保存した論文を後から検索し、学習素材として再利用したい」、14 節の Library View「検索・管理に適した整然UI」、1 節の Canvas「保存データ」。コーパス全体の検索はどこにも書かれていません。

**これは書き漏らしではなく、製品の判断だと読みました。** 16 節は発見を**フィード**として設計しています — 70/20/10、多様性制御、全カードに理由。全文検索の箱は、人がフィードの**代わりに**手を伸ばすものです。付けると、このアプリが中心に据えた仕組みが、他の全ツールが持っている仕組みに静かに置き換わります。コーパス検索が欲しくなったら、この機能の副作用ではなく、意図した第 2 の機能として作るべきです。

**全文検索ではなく部分一致にしました。** PostgreSQL の `to_tsvector` は英語の大量文書には正しい道具で、ここでは 2 つの理由で違います。個人のライブラリは数百行であって数百万行ではありません。そして**テキストが混ざっています** — abstract は英語ですが、読者が書くメモは日本語です。`to_tsvector` は日本語を分割できないので、長い日本語メモの中の「測度」は**一致しません**。`ILIKE` なら見つかります。

**埋め込みも使いません。** hashed bag-of-words（D-016）なので、2 語のクエリはほぼ空のベクトルになります。意味検索を名乗って、部分一致より悪い順位を返すことになります。

**`%` と `_` はエスケープします。** `O(n_max)` を探した読者はその文字を literal に意味しています。エスケープを忘れると `_` が任意の 1 文字に化けて、**検索が黙って訊かれた以上を返します** — 何も返さないより悪い壊れ方です。

**順位付けは SQL 側でやりました。ここで自分のバグを 1 つ潰しています。** 最初は SQL で `LIMIT` してから Python で「タイトル > 著者 > 会場 > メモ > abstract」に並べ替えていました。**`LIMIT` は SQL で起きるので、どの行を残すかはデータベースが好きな順で決めます** — タイトル一致が落ちて abstract 一致が残り、残った中で「正しく」並べ替えられる。順序は正しく見えて、いちばん良い行だけが居ない。テストで見つけたので、`CASE` で順位を SQL に持たせ、**並べてから切る**ようにしました。

**どのフィールドで当たったかを返します。** 6 節がカードに理由を求めているのと同じ理由です。理由の見えない一致は、検索が当てずっぽうをやっているように見えます。

---

## D-042 数式の全画面表示は 1 つだけ持ち、コピーは LaTeX のみ

**決定** 11 節の「タップで全画面 / フォント拡大 / LaTeXコピー」を実装。全画面シートはアプリ全体で 1 つ（`FormulaZoomProvider` を root に置く）、コピーボタンは 1 つで、コピーするのは LaTeX。

**シートを 1 つにした理由。** 数式ごとにシートを持つと、導出タブだけで `Modal` が 6〜7 個になります。開くのは常に 1 つなので、root に 1 つ置いて context 越しに開きます。副次的にこちらが本命で、**画面ごとに配線を思い出さなくても、アプリ内のどの数式も全画面になります**。11 節は全画面を「数式の性質」として書いていて、特定の画面の機能としては書いていません。

**「コピー」と「LaTeXコピー」は 1 つにまとめました。** 11 節はこの 2 つを別項目として並べていて、素直に読めば「数式をテキストとして」と「ソースとして」です。**数式の場合、その 2 つは同じ文字列です** — 11 節の 1 行目が「正規データはLaTeX文字列とする」と書いており、このアプリに LaTeX → Unicode の変換器はありません。同じ文字列をクリップボードに置くボタンを 2 つ並べるのは、**選択肢があるふりをするメニュー**です。1 つにして、何をコピーするかをラベルに書きました。平文の表現ができたときに 2 つ目のボタンを作ります。

**フォント拡大は倍率で、絶対サイズではありません。** 22 節の Dynamic Type がすでにアプリ全体を読者の設定で拡大しています。ここで絶対 pt を指定すると、**いちばん拡大が必要な要素に限ってその設定を無効化する**ことになります。倍率なら読者が選んだ大きさの上に乗ります。

**由来ラベルをシートに入れました。** 11 節は全数式に由来（Original / Verified step / AI explanation / Assumption / Human reviewed）を求めています。全画面はそれを最も失いやすい場所です — 数式がラベルを表示していたカードから離れるからです。だから呼び出し側が近くに描いてくれることを期待せず、シート自身の一部にしました。

**テストが自分のアクセシビリティのバグを見つけました。** 最初、`onPress` は WebView の上に重ねた透明レイヤだけに置いていました（WebView はタッチを飲み込むので、その層は必要です）。しかしスクリーンリーダーが実際に activate するのは**外側の要素**で、そこには `onPress` がありませんでした。つまり **VoiceOver は「ボタン」と読み上げるのに、ダブルタップしても何も起きない**状態でした。いまは外側も `Pressable` で、両方が同じハンドラを呼びます。20 節の「ジェスチャーに非ジェスチャーの代替を」は、こういう静かな壊れ方も含みます。

**Abstract の数式はタップで全画面になりません。** カードの本文では、文タップは 7 節の「選択して翻訳」に割り当てられています。数式にだけ別の意味を与えると、**同じ本文の中でタップの意味が場所によって変わります**。全画面が要るのは Focus Mode の数式で、そこでは数式そのものが主題です。

**コピーの成否を報告します。** `Clipboard.setStringAsync` は成功を bool で返すので、それを握り潰さずに出しました。コピーできたと信じた読者は、**前にクリップボードにあったものをノートに貼って、気づきません**。

---

## D-043 数式も web で描画する（`MathFrame` の分割）。ついでに「はみ出し」を測って伝える

**きっかけ** 全画面表示をブラウザで確認しようとしたら、数式の場所に **"React Native WebView does not support this platform."** と出ました。`react-native-webview` に web 実装が無いためで、これは私の変更で壊れたのではなく、**Focus Mode の数式は一度も描画されたことが無かった**ということです。

**これは見た目の問題ではありません。** 実機確認は止まっています（D-030）。つまり**ブラウザが唯一の確認手段**で、そこで描画されないということは、Phase 4-B と 4-C の数式は**どこでも確認できていなかった**ことになります。テストは通っていました — jest の WebView はモックで、モックは「レンダラが報告してこない」状態を再現するものだからです（意図通り）。緑のテストは、この空白を一度も指していませんでした。

**直し方は Abstract と同じ形にしました。** `AbstractFrame.tsx` / `AbstractFrame.web.tsx` の分割がすでにあったので、`MathFrame.tsx` / `MathFrame.web.tsx` を同じ形で作り、`MathView` はどちらかを使うだけにしました。web 側は **blob URL の iframe**です（`srcdoc` だと meta CSP でスクリプトが走らず、KaTeX が黙って LaTeX ソースにフォールバックする — D-035 と同じ罠）。文書側の `post()` にも `window.parent.postMessage` の分岐を足しました。sandbox された iframe はこれ以外で親と話せません。

**大きくしたら式が切れて、それで 2 つ目の問題が見えました。** 4 段階目にすると式が画面幅を超えます。`#root` には `overflow-x: auto` があるので**スクロールはできて**いました（測って確認: `scrollWidth` 575 / `clientWidth` 380、右端まで動く）。問題は**それが見えないこと**です。オーバーレイ型のスクロールバーは触ったときしか出ないので、**「スクロールできる」ことを伝える役目を果たしません**。切れた式は、続きがあるのではなく**描画が壊れて見えます**。

3 つやりました。**スクロールバーを常時描画**（`::-webkit-scrollbar` + `scrollbar-width: thin`）。**はみ出しを文書側で測って報告**（`scrollWidth > clientWidth`）— これはレンダラの中でしか分からず、native 側からは見えません。そして**シートに一文出す**：「横スクロールできます。小さい段階を選ぶと全体が入ります」。

**後半の一文が 20 節の要求です。** 横スクロールはジェスチャーで、代替が要ります。新しいコントロールを足すのではなく、**すでにある大きさの段階がその代替**だと明示しました。段階 1 に戻せば式は全部入ります。ブラウザで 1〜4 を切り替えて、1・2 では出ず 3・4 で出ることを確認済みです（推測ではなく実測に連動しています）。

---

## D-044 Visual regression は「固定の内容 × 本物のコンポーネント」で撮る

**決定** `/dev/visual` に部品を並べたギャラリー画面を置き、`scripts/visual-regression.mjs` が 8 条件を撮って、コミット済みのベースラインと画素比較する（`npm run visual` / `npm run visual:update`）。

**内容は固定、部品は本物**という組み合わせが要点です。フィードはランダムなので、実データを撮ると**毎回違う画像**になり、その suite は 1 週間で誰も見なくなります。かといって見た目だけ似せた張りぼてを並べると、**本物が変わってもベースラインは動かない** — スクリーンショット suite が存在する理由そのものを失います。だから内容だけを定数にして、描くのは出荷されるコンポーネントにしました。

**条件は URL から取ります。** ライト/ダーク、320/390/820px、Dynamic Type 1.0/1.5/2.0、Reduce Motion、日本語/英語。ギャラリーは自分の `ThemeProvider` を入れ子で持ち、**単体テストが使っているのと同じ seam** を強制するので、「ダーク」「大きい文字」の意味がテストとギャラリーで食い違うことがありません。

**最初の実行で本物のバグが出ました。** Dynamic Type 2.0 で、スクリーンショットの幅が viewport より 31px 広くなりました。**操作ボタン（見送る / 原文 / 保存）が画面からはみ出し、左端が切れていました。** このボタンは 20 節が要求する**スワイプの非ジェスチャー代替**なので、いちばん壊れてはいけないものです。`flexShrink` と `flexWrap` で 2 行に折り返すようにしました。手で気づけたとは思いません — 2.0 倍で数式の下まで見る人はいません。

**harness 側で 2 回、静かに嘘をつかれました。両方とも「空白は合格に見える」形です。**

1. 最初は viewport ぶんしか撮っておらず、セクション 4 以降が**一度も撮られていませんでした**。ギャラリーが `ScrollView` だったので、`fullPage` でもスクロール領域の外は写りません。`View` に変えてページ自体を伸ばしました。
2. 数式が**全条件で真っ白**でした。blob URL の iframe は cross-origin なので Chromium では別プロセスになり、`fullPage` スクリーンショットの再描画に**追従しません**。空白領域は空白のベースラインと綺麗に一致するので、**差分ゼロ＝合格**に見えていました。`--disable-features=IsolateOrigins,site-per-process` で同一プロセスに戻して解決。スクリーンショット用の harness を localhost に向けている限りは許容できますが、アプリ側でやることではありません。

**`npm run ci` には入れていません。** ベースラインはフォントのラスタライズに依存するので、別のマシンでは画素一致しません。共有 CI に載せるなら固定のコンテナ image が要ります。いまは**ローカルのゲート**で、意図した変更は `--update` で受け入れます。ベースラインを受け入れるのはデザインを受け入れることなので、スクリプトはその一文を毎回出します。

**閾値は 2 段構えです。** 画素あたりの色差 0.15（グリフの縁のアンチエイリアスは吸収し、文字色の変更は吸収しない）と、フレーム全体の 0.2%（レイアウトが動けば必ず超える）。サイズが変わった場合は比較せず即失敗にします — 高さが変わったこと自体が知らせるべき事実だからです。

---

## D-045 本文のライセンスは論文メタデータのライセンスではない（数式パイプラインの門）

**決定** `FullTextRecord` に**本文自身のライセンス**を持たせ、`services/fulltext.py` の門を通ったものだけが数式パイプラインに入る。`Paper.license_id` は使わない。

**この区別が今回の中心です。** arXiv の**メタデータは CC0** です。しかし**本文は著者が選んだライセンス**で、arXiv の論文の大半は arXiv 非独占ライセンスです。それは **arXiv が配布することを許すもの**で、第三者がソースを解析して導出を再公開することについては何も言っていません。`Paper.license_id` を再利用すると、メタデータの CC0 を読んで**arXiv の全論文が通ります** — 21 節の「ライセンス不明の本文断片を数式カード化しない」が禁じているのは、まさにこれです。

**既定は拒否です。** 判定は「禁じるものがあるか」ではなく「**許すものがあるか**」。誰も認識していないライセンス、provider が空にしたライセンス欄、主張を確かめる原文リンクの無いレコード — 全部 no です。この向きが重要なのは、**間違いが静かな方向にしか出ない**からです。解析する権利が無かった本文から作ったカードは、権利があった本文から作ったカードと**見た目が同じ**です。

**未知のライセンスは実行時に調べません。** 許可リストにも拒否リストにも無いものは拒否します。あるライセンスがこれを許すかどうかの判断は、**人が一度読んで決めること**であって、文字列比較が推測することではありません。

**拒否理由に名前を付けました。** 「ライセンス不明」で片付けず、arXiv 非独占・ND・NC をそれぞれ理由付きで拒否リストに載せています。「未知」と書くと、誰かが調べに行って「大丈夫そうだ」と結論します。NC を今のうちに外しているのは、21 節が商用化前の個別確認を求めていて、**その時点で作り直すより今除外するほうが安い**からです。

**抽出は主張をしません。** `text/latex_document.py` が返す式はすべて `source_exact`（原稿からのバイト単位のコピー）です。変形の提案とその検証は後段で、`unverified` から始まります。抽出と検証を分けているから、UI が「どちらか」を言えます。

**式番号は著者のものだけを使います。** `\tag` があればその番号、無ければ `None` です。LaTeX は組版時に採番しますが、**私たちは組版していません**。こちらで採番した番号は論文の番号と見分けがつかず、**読者を違う行へ送ります**。

**記号表は狭い形しか読みません。** 「where $x$ is the ...」だけです。`$...$` を全部拾って定義と呼ぶ実装は簡単で、**自信のある嘘で埋まった記号表**を作ります。読者は読み取られた意味と推測された意味を区別できません — 0 節がデータモデルに要求しているのは、まさにその区別です。**取りこぼしは安全、間違いは危険**なので、狭いほうを選びました。

**空行の扱いで自分のバグを 1 つ潰しました。** 段落を空行で分割して、式の直前・直後の要素を取っていました。ディスプレイ数式は慣例として**前後を空行で囲む**ので、隣接する要素は**常に空**です。結果として全部の式で文脈が空になり、**記号表と参照リストが同時に、静かに空になっていました**。テストではなく、fixture に対して走らせて出力を見て気づきました。

---

## D-046 LaTeX を読んで検算する。ただし読めないものは黙って落とさず、拒否する

**きっかけ** D-028 が穴を記録していました — `mathcheck` は式の**横に**書かれた機械可読形を評価するので、**チェックが LaTeX と食い違いうる**。画面の式とは別の主張が検証済みバッジを得られる、という穴です。`text/latex_expression.py` はそれを読み始めるものです。

**受け入れるより遥かに多くを拒否します。これが設計です。** `\int` を黙って落とす翻訳器は、積分の恒等式を**算術の恒等式に変えて**標本を取り、合格を返します。式は**別の主張で得たバッジ**を身に付けます。だから構文は「正確に理解できる」か「式全体を拒否」かのどちらかです。`None` が普通の答えで、それは安全です — 未検証の step は非表示なので、読者は何も失いません。

**自分のバグを 3 つ、テストと実行で見つけました。どれも「静かに間違える」形です。**

1. **`I(a)` を `I * a` と読んでいました。** LaTeX の文法では並置は積で、論文では関数適用です。**マークアップのどこにもどちらか書いていません**。積として読むと `I * a` を相手側と比較して、**論文が言っていない主張についての判定**を出します。いまは識別子の直後の `(` は拒否します。数字の直後は曖昧でないので通します。
2. **`\lambda` が Python の予約語 `lambda` になっていました。** 論文で最も多い記号の 1 つで、生成された式は構文エラーになります。呼び出し側は「確認できない」ではなく**クラッシュ**を受け取ります。予約語は末尾に `_` を付けて回避します。
3. **`\left( ... \right)` を全部拒否していました。** `\left`/`\right` を装飾コマンド扱いにしていたので、`_term` が `\right` を**並置された因子として消費**していました。区切り記号として明示的に扱います。

**検算の考え方を一度作り直しました。** 最初は 2 つの式それぞれを「箱の上の恒等式」として標本を取っていました。しかし**論文の式は定義であって恒等式ではありません**。`y = (a+b)/2` は `y` を拘束するので、`a`・`b`・`y` を独立に振ると制約はほぼ成り立たず、**このチェックは失敗しかしません** — しかも**正しい数学に対していちばん大きな声で失敗します**。fixture に対して走らせて出力を見るまで気づきませんでした。

いまは**代入**します。前の式が 1 つの変数について解けていれば、残りを標本し、その変数を前の式から計算し、後の式がその点で成り立つかを見ます。fixture には**わざと間違えた行**を置いてあります — 通るものしか返さないパイプラインは、チェックが働くことを何も示さないからです。実行すると、正しい step は `numerically_spot_checked`、間違った step は**どの点で食い違ったかを添えて**拒否されます。

**これは「導出できる」ことの確認ではありません。** 確認できるのは「2 つの行が同じ関係を表しているか」です。著者が書いた操作が実際に行われた操作かは分かりません。だから上限は `numerically_spot_checked` で、`mechanically_verified` は名乗りません（次元チェックも記号操作も走っていないので）。

**散文は決して検証になりません。** 「両辺を 2 乗して」は著者が**そうしたと言っている**証拠であって、それが正しいことの証拠ではありません。関係ラベルは著者の文の要約であり、結論ではありません。ラベルが付かない（`None`）のは普通の答えです。

**そのラベル付けでも 1 つ間違えました。** `\bfactor` が「contributes a factor of $2\pi$」に一致していました。名詞で、操作ではなく結果の説明です。**間違った文に反応する手がかりは、自信のある間違ったラベルを作ります**。動詞形だけに絞りました。

**中間式は生成しません。** D-028 のとおりです。CAS が無い以上、論文の 2 行の間にこちらが作った式は**論文の服を着た捏造**です。生成するのは行と行の**リンク**だけ — 操作（著者の文の引用）と検証の試行です。

---

## D-047 レビューキューは「必要なカードだけ」を、理由を添えて並べる

**決定** `services/review.py` と `papermatch review`。12 節の手順 10「**必要なカードのみ**人手レビューへ送る」を、triage（そのカードに人が要るか）と優先順位付けとして実装する。

**「必要な」が仕事の全部です。** 全カードが入るキューは誰も開きません。まともなカード 40 枚をスクロールして壊れた 1 枚に辿り着く査読者は、**5 枚目で丁寧に読むのをやめます**。だからこのモジュールの本当の仕事は、何を**入れないか**を決めることです。

**キューに入る理由は必ず言葉で持ちます。** 誰も検算できないスコアではなく、カードについての事実です — 読者の報告がある、機械的検証を通った変形が 1 つも無い、本文が AI 生成、検証を通らなかった変形がある。優先度は理由から**導出**しています。別の数値にすると理由と食い違い得て、そうなるとキューの説明が信用できなくなります。

**`human_reviewed` はここからしか到達できません。** `mathcheck.combined_status` はこれを返せず、返せないと明記しています — どれだけ計算しても「人が見た」ことは establish できません。承認だけが経路であり、それが UI のラベルに意味を与えています。

**承認は未検証の変形を洗浄しません。** 12 節は未検証の step を既定で隠すので、**査読者はその step を見ていません**。カード全体を承認したからといってそれを `human_reviewed` にするのは、**見せられていない数学を人が確認したと主張する**ことです。ラベルの価値は「そこに書いてあることが本当」であることだけなので、これは最悪の壊し方です。回帰テストは、ガードを外すと落ちることを確認済みです。

**却下は隠すのであって、消しません。** 何が、なぜ却下されたかの記録だけが、**来週パイプラインが同じものを再提案するのを止めます**。

**「要修正」には必ずメモを求めます。** どこが、と言わない「要修正」は決定ではなく先送りです。カードは draft に戻さず in_review に留めます — 一度見られたという事実を失うと、新品として triage をもう一周します。

**読者の報告は証拠であって決定ではありません**（D-038）。このキューでの順位を上げますが、それ自体では検証状態を動かしません。テーブルを分けているのは、まさにこのためです。

**いまのコーパスでは全 5 枚がキューに入ります。** 全カードの本文が AI 生成で（D-022）、まだ誰も読んでいないからです。これは「絞り込みが効いていない」のではなく**データがそうである**という事実で、docstring にもそう書きました（最初は「None が普通の答え」と書いていて、実データはそれを否定していました）。`ai_authored_body` はキューの**床**で、実際に効いているのは順序です — 報告 3 件のカードが先頭、検証ゼロのカードが 2 番目に来ます。

**決定は CLI のフラグにしていません。** 承認はカードの step が `human_reviewed` を名乗れるようにする行為なので、一覧コマンドから 1 キーストロークの距離にあるべきではありません。`papermatch review` は読むだけです。

---

## D-048 Canvas の座標は t-SNE でも UMAP でもなく、データを見ない射影で決める

**きっかけ** 13 節は「意味ベクトルの近さで配置」と言った直後に、それをほぼ完全に縛ります — 新規論文は近い領域に加える、**全体再配置を最小化**、手動配置を尊重、配置アルゴリズムの版を保持。

**この制約が t-SNE と UMAP を排除します。ここが今回の判断の全部です。** 512 次元を平面に落とすと言われて誰もが最初に思いつくのがこの 2 つで、この用途にはどちらも不適です。**出力が入力集合全体に依存する**ので、論文を 1 本足すと**他の全タイルが動きます**。しかも実行ごとに決定的ですらありません。保存するたびにタイルが徘徊する canvas は**場所ではありません** — 13 節が求めているのは明らかに場所、戻ってきて見覚えがあるものです。

**なので射影はデータを一切見ません。** 分野は id から決まる円周上の固定点に錨を下ろし、埋め込みは定数で seed した固定の擬似乱数基底を通します。どちらも「どの論文が存在するか」に依存しないので、**既存のタイルは動きようがありません** — 安定性が構造的で、走らせて祈るものではありません。代償は UMAP ほど綺麗にクラスタが見えないことで、これは承知の上です。13 節の要求は安定した平面であって最密のクラスタではないし、そもそも埋め込みは hashed BoW（D-016）なので、近傍法が見つけるクラスタはそこに存在しません。

**モザイクは重なってはいけませんが、衝突を押しのけて解決すると上の全部が崩れます。** なので格子に量子化して、**最も近い空きセル**を取ります。新しいタイルは誰も持っていないセルしか取れないので、何も押し出されません。実データで確認済みです — 12 枚配置してセル重複 0、1 本追加して**動いたタイル 0 枚**。

**色は Oklab で混ぜます。13 節が「RGB単純平均を避ける」と明記していて、その理由は具体的です。** sRGB はガンマ符号化されているので、チャンネルを平均すると**光ではなく符号を平均する**ことになり、結果は**両親の中点より暗くなります**。このパレットで測ると、`cs` と `cond-mat` の素朴な平均は知覚明度 0.51、両親と真の中点は 0.59 付近です。

**これが趣味の問題を超える理由**：13 節は**明るさを「最近の活動」に使っています**。暗くなった混色は醜いだけでなく、**別の変数について誤った値を静かに報告しているタイル**で、読者には見分けがつきません。

**ここで自分の思い込みを 1 つ訂正しました。** 最初、docstring とテストに「青と黄を混ぜると緑になる」と書きました。**間違いです。** それは*絵の具*（減法混色）の話で、Oklab では補色同士は中性に向かいます。テストが落ちて（知覚彩度 18 < 素朴な平均 42.5）気づきました。**測って初めて分かる種類の間違い**で、そのまま出していたら色彩について誤ったことを主張するコメントが残っていました。いまは実測に基づく本当の性質 — 明度が保存されること — を主張し、テストもそれを固定しています。

**色だけに意味を載せてはいません**（20 節）。タイルの分野は平面上の位置（どの島にいるか）でもあり、近景ではラベルでもあります。

---

## D-049 Canvas 画面：遠景は島、近景はタイル、拡大したらパンする

**決定** `GET /canvas` / `PATCH /canvas/{paperId}` と「眺める」タブ。13 節のズーム段階を、**描くものを変える**ことで実装する。

**遠景でタイルを描くのは無理です。実際に描いてみて分かりました。** 平面は約 70 単位幅で、格子は 1 単位刻みです。全体を電話の画面に収めると **1 セルが約 5 ポイント**になります。最初はそこにタイルを描こうとして、全部が最小タップサイズに張り付き、**配置は正しいのに重なって**表示されました。13 節はそこで何を描くべきかを最初から書いています — **遠景：分野島と件数**。いまは島を円で描き、中に件数を入れます。

**拡大したら平面は収まらなくなるので、パンします。** 3 段階目で平面は 420 ポイントの画面に対して約 1600 ポイントになります。最初のバージョンは中央だけを切り出していて、**ヘッダーが「タイル 8 枚」と言っているのに平面は空**でした。全部がビューポートの外にあったからです。いまは内容座標で並べて 2 軸のスクロールに入れているので、置いたものは必ず届きます。

**倍率に上下限が要ります。ここでも空の平面を出しました。** ビューポートに合わせる計算はタイルの広がりで割るので、**2 枚に絞り込むと広がりが約 1 単位**になり、倍率が 1 単位あたり約 2500 ポイントに跳ねて、タイルが画面より大きく描かれ、また平面が空になりました。上限と下限を入れてあります。

**タイルはセルより大きくしません。** 配置が保証しているのは**セルが重ならない**ことなので、セルより広いタイルはどれだけ丁寧に置いても隣と重なります。最初はポイント単位で独立に大きさを決めていて、**下の配置は完全に正しいのに画面上は重なって**いました。`tile_size` の上限を 0.95 にしてあり、テストで 1.0 未満を固定しています。

**選択は変形であって再配置ではありません。** 選ばれたタイルは 1.15 倍に浮き、近くのタイルは少し離れますが、**下の座標は動きません**。選択を解くと全部が元の位置に戻ります — そもそも動いていないからです。Reduce Motion では移動だけを外します。枠・明度・下のパネルが選択を伝えるので、意味は失われません。

**島をタップすると絞り込んで 1 段階寄ります**（13 節のクラスタズーム）。**絞り込みは隠すだけで、残ったタイルは動きません** — 平面は場所なので、絞り込むたびに別の場所になってはいけません。

**タイルを動かすのは方向ボタンです。** ドラッグは、スイッチコントロールやキーボードの利用者が**唯一まったく実行できない**操作です。20 節の「ジェスチャーに非ジェスチャーの代替を」がいちばん効くのがここなので、ボタンを主にしました。最初、そのボタンのラベルを「この位置は自分で決めたものです」にしていました — **状態の説明であって操作の名前ではありません**。上へ / 下へ / 左へ / 右へ に直しました。

**タイルの文字色は測って決めます。** 分野色は明度の幅が広く（`stat` は明るい琥珀）、白に固定すると明るい側でコントラスト基準を割ります。**分野を名指しする唯一のラベル**が、いちばんそれを必要とする読者に届かなくなります。`readableOn()` が測って黒か白を選び、全分野色で 3:1 以上をテストで固定しています。

---

## D-050 Canvas は Saved の中の表示切替にする（別タブをやめた）

**決定** 「眺める」タブを廃止し、Saved 画面に「一覧 / Canvas」の切替を置く。選択中の論文は画面が持ち、どちらの表示にも渡す。

**理由は仕様書にありました。** 14 節は Canvas と Library を「同じ保存データを、検索・管理に適した整然UIで表示する」もの、つまり**同じデータの 2 つの表示**として書いています。2 節の利用フローも「SavedをLibraryまたはCanvasで閲覧」で、1 節は両者を「滑らかに切り替えられる」ことを求めています。別タブにすると、同じ論文を持つ**2 つの場所**になり、別の製品になります。

**選択を画面が持つのが要点です。** 14 節は「逆方向も位置関係を失わない」と書いています。各表示が自分の選択を持つと、切り替えた瞬間に読者は自分がどこにいたか分からなくなります。いまは平面でタイルを選んで一覧に戻ると、その行に枠が付いています。

**スクロール位置の同期はやめました。** 最初 `initialScrollIndex` で選択行まで飛ばしていましたが、`getItemLayout` が無いと信頼できず、**リスト末尾の論文を選ぶと全部を通り過ぎて、カード 1 枚だけの空に見える画面**になりました。枠で示すだけにしています。

**連続変形（タイルが行へモーフする）は未実装です。** 切替と選択の引き継ぎは動きます。20 節が Reduce Motion で即時切替を許しているので、その経路は満たしています。

---

## D-051 共有画像：私的な項目は既定で除外し、除外したことを画面に書く

**決定** `buildShareCard(paper, saved, options)` と `ShareSheet`。タイトル・著者・年・掲載先は既定で入り、**自分のメモと保存理由は既定で入らない**。閲覧履歴は選択肢として存在しない。原文リンクとライセンスは選択できず、常に入る。

**既定の向きがこの機能の中身です。** 21 節は「共有画像から私的データを既定で除外」と書いています。既定を逆にした場合の間違いは**取り返しがつきません** — 自分のメモが隅に写っていることに気づくのは、画像がすでに他人のタイムラインに乗ったあとです。逆向きの間違い（メモを入れたかったのに入っていない）は、チップを 1 つ押せば直ります。この非対称性がそのまま既定になっています。

**除外したことを画面に書きます。** 「画像に含めていないもの: 自分のメモ、保存した理由」を出しています。黙っていると、**メモが除外されている状態と、そもそもメモを書いていない状態が区別できません**。読者が確認できない安全策は、安全だと思い込ませるだけです。

**閲覧履歴は選択肢にしていません。** 15 節は既定で除外するものとして挙げていますが、共有画像に「5 回開いた」を載せたい場面が製品側に一つもありません。**誰も要らない選択肢は、誰かが誤って on にできる選択肢**です。パラメータ自体を作っていません。

**出力は SVG（ビットマップではない）です。** テキストなので、テストで「メモの文字列が含まれないこと」を直接検証できます。ラスタライザだと、この規則は**目で見る以外に確認する方法が無くなります**。ピクセルが要る配布先では呼び出し側でラスタライズします。文字列はすべて XML エスケープしています — タイトルの `<` が壊す先は、**それに気づける人からいちばん遠くまで運ばれる成果物**です。

**書き出す画像はライトテーマ固定です。** 読者がダークテーマで書き出すと、暗い灰色の画像が、受け取った側のアプリのどんな背景の上に置かれるか分かりません。画面上のプレビューはアプリのテーマに従います（そちらはこの画面の中にあるので）。

**プレビューは書き出す `ShareCard` そのものから描いています。** プレビューと書き出しが別経路だと、食い違いがいちばん困る場所で食い違います — 読者がプレビューを見るのは、共有が取り消せないからです。

**確認中に別の不具合を見つけて直しました。** Saved 画面を直接開くと、トークンの読み出しが終わる前に `GET /saved` が飛んで 401 になり、その結果が**「まだ保存した論文はありません」として表示されて**いました。保存が消えたと読者に伝える表示です。いまはセッションの起動が済むまでリクエストを保留し、取得に失敗したときは空ではなく失敗として（再試行つきで）表示します。

---

## D-052 タイルと行の連続変形：両端は描いた側が申告し、届かないときは変形しない

**決定** `morph.ts`（幾何）/ `MorphTargets.tsx`（両端の測定）/ `MorphLayer.tsx`（実際の移動）。Library と Canvas を切り替えるとき、**選択中の論文**だけが片方の位置からもう片方の位置へ移動する。

**このアニメーションは装飾ではありません。** 14 節は 2 つを同じ保存データの表示と定めていて、読者に伝わらなければならないのは「**平面上の四角と一覧の行は同じ 1 本の論文だ**」ということです。切り替えが単なるカットだと、読者がそれを自分で突き合わせることになります。選択が無いときに変形しないのも同じ理由で、そもそも「私の論文はどこへ行った」という問いが発生していません。

**両端の形は、描いた側が申告します。** タイルは角丸の正方形、島は円、行はカードです。矩形からは区別できないので、**「小さくて正方形だから円だろう」と推測すると、論文が 1 本しかない分野でちょうど外れます**。登録する側が半径と色を一緒に渡しています。

**遠景では島が着地点です。** その倍率では論文に自分のタイルがありませんが、**どこにも無いわけではなく**、自分の分野の島の中にいます。ここで変形を拒むと、いちばん普通の操作 — 既定の倍率で Canvas に切り替える — が一度も連続変形にならないことになります。

**届かないときは変形しません。** 片方の矩形が画面外か大きさ 0 のとき（絞り込みで消えている、検索結果に入っていない、まだレイアウトされていない）は、そのまま切り替えます。**推測した位置から飛んでくる図形は、読者が見ていた場所について嘘をつきます** — 何も動かないより悪い結果です。

**色は Oklab を通ります。** sRGB のまま補間すると中間が両端より暗くなります（D-048）。移動の途中でタイルが一瞬暗くなるのは、**読者が目で追っているまさにその瞬間に、選択が外れたように見える**ということです。

**Reduce Motion では即時切替です。** 20 節は「短いフェードまたは即時切替」を許しています。速く飛ばすだけの「軽い変形」は、その設定が消そうとしている動きそのものです。

**実装中に 2 つ直しました。どちらも動かしてみて分かったものです。**

1 つ目：**切り替え先を測るとき、切り替え元がまだ登録されたままでした。** その結果 `awaitTarget` が「これから離れる場所」を返し、図形は行から行へ「移動」して、読者には**新しい画面の上に色の付いた箱が現れてその場で縮む**ように見えました。両端とも正しい矩形なので型では防げません。いまは 2 回の測定の間に登録を落としています。後片付けは同一性で守っているので、切り替え元の解除が切り替え先の登録より後に走っても消しません。

2 つ目：`visual-regression` の `light-wide` の基準画像に、**Abstract 本文が真っ白なまま記録されていました。** 数式を含む本文は blob URL の iframe で描かれ、Chromium は画面外の iframe を描画しません。`fullPage` はビューポートを変えながら継ぎ足すので、そのとき描かれていない領域は白いまま入ります。**白は「合格に見える唯一の失敗」です** — 白い基準画像と白い出力は綺麗に一致します。高さの報告を待つだけでは足りません（高さは `postMessage` で先に届き、描画はその後です）。いまはページ全体が入る大きさまでビューポートを広げてから撮り、さらに**連続 2 枚が完全に一致するまで撮り直します**。3 回連続で差分なしを確認し、基準画像は数式が描かれた状態に取り直しました。

---

## D-053 論文関係：類似度が単独で出せるラベルは「類似」だけ

**決定** `services/relations.py` と `GET /papers/{id}/relations`、Canvas 選択パネルの `RelationList`。基礎 / 後続 / 対立 / 類似 の 4 つを、**それぞれが要求する根拠がそろったときだけ**付ける。

**17 節の一文がこのモジュールの全部です。** 「単なる類似度だけで関係ラベルを断定しない」。抄録が似ているという事実から言えるのは「類似」だけで、**基礎でも後続でも対立でもありません** — どれも類似度には見えないものについての主張です。似ていて古い論文を「基礎になっている研究」と呼ぶのは、両方がマルコフ連鎖の話だというだけの根拠で「この論文はあれの上に建っている」と読者に伝えることです。根拠が無いラベルは**出しません**。「いちばんそれらしいもの」を返す経路はありません。

**方向は引用から読み、日付からは読みません。** 基礎と後続は同じ引用辺を反対側から見たもので、どちらかは誰が誰を引いたかだけで決まります。プレプリントは後から出版される論文を引けるので、**日付は裏付けにしかなりません**。日付が引用と食い違うときは食い違いとして記録し、確信度を下げるだけでラベルは反転させません。

**対立は誰かがそう書いていることを要求します。** 「結論が違う」をメタデータから検出する方法はありません。検出できるのは片方の本文が**そう言っている**ことなので、対立には対比を表す語を含む言及文が要ります。ここでいちばん狭くしてあるのは意図的で、**対立していない 2 本を対立していると伝えると、読者は存在しない議論を探しに行きます**。

**確信度は画面に出しません。** 「0.85」は読者が何かを判断できる情報ではなく、しかも**種類の違う根拠から出た 2 つの数字を並べて比べさせます**。代わりに根拠の種類（引用による / 本文の言及による / 内容が近い）を文で書いています。

**空は本物の答えです。** 根拠が何も無ければ空のリストを返し、画面には「似ているだけの論文はここに出しません」と書きます。黙って空にすると、**「関係が無い」と「調べていない」が読者から区別できません**。

**関係は毎回計算し直し、保存済みの行は置き換えます。** 根拠が支えなくなった関係は削除します。古い関係が残っていると、**すでに無い根拠で「基礎になっている」と読者に伝え続けることになり、画面にはそれが古いとはどこにも書いてありません**。

**途中で 1 つ型を直しました。** `packages/shared-types` の `PaperRelation.evidence` はサービスより前に書かれていて、キー名（`citationDirection` / `mentionSnippet` / `model`）がサーバーの実際の出力と違いました。**誰も使っていない型なのでコンパイルは通り、使い始めた瞬間に嘘になる**種類のものです。実際の形に合わせました。

**まだ埋まっていない入力が 1 つあります。** 分類器は言及文を受け取れますが、**全文を保存する仕組みが無いので現状は常に空**です。したがって「対立」は実データでは出ません。21 節のライセンス判定（`services/fulltext.py`）はすでにあるので、全文の保存が入れば接続するだけです。TASKS.md に未了として残しました。

---

## D-054 読書ルート：持っていないものは段に書かない

**決定** `services/reading_path.py` と `GET /papers/{id}/reading-path`、Saved 行の「読むルート」シート。17 節の 4 つ（全体を知る / 数式を追う / 結果だけ見る / 引用に使えるか確認）を、**このアプリが実際に持っているものだけ**で組み立てる。

**17 節の例がそのまま罠でした。** 「Abstract → Figure 1 → Introduction 1〜3段落 → Eq. 7 → Conclusion」と書かれていますが、**本文は持っていません**。その論文に Figure 1 があるのか、序論が何段落あるのか、Eq. 7 が存在するのか、一度も見ていません。「Figure 1 を見てください」と書いたルートは構造を捏造していて、**読者はそれを、我々を信用して PDF を開いたあとに、無いものを探しながら気づくことになります**。

**そこで各段に `held` を持たせました。** 真なら中身がアプリの中にあります（判定済みの Abstract 文、抽出済みの数式、メタデータの値）。偽なら「原論文を開いて見てください」という指示で、それ以上のものには見せません。画面でも枠線の色を変えて、**どこでアプリの知識が終わるのかが、最後まで読まなくても分かる**ようにしています。

**目的はフィルターであって書き換えではありません。** 4 つとも同じ材料から、論文が提示している順のまま選ぶだけです。「重要度順」に並べ替えると、読者が自分で決めた目的の上に、論文についての二番目の意見を重ねることになります。

**ルートが短く返ることは正しい結果です。** 判定済みの文も抽出済みの数式も無い論文では、メタデータの段とリンクだけが返ります。**「序論をざっと読む」のような一般論で長さを稼ぐと、どの論文のルートも同じになり、どれ一つ本当ではなくなります**。足りないものは `missing` として名指しし、画面にも書いています。

**引用チェックだけは順序が逆です。** 掲載先・版・ライセンス・オープンアクセス（と、あるときだけ撤回状態）を先に出します。引用してよいかの判断は、その論文がどうやって結論に至ったかではなく、**使える形で存在しているか**の話だからです。ライセンス不明は行ごと落とさず「不明」と出します。落とすと、そもそも確認していないように見えます。

**撤回状態は、あるときだけ出します。** 全部の論文に「撤回なし」を付けると、**本当に撤回された 1 本のバッジを読者が読み飛ばすようになります**。

**表示中に 1 つ壊しました。** 行の操作ボタンが 5 個になり、`flex: 1` で等分していたので 390pt では**ラベルが 1 文字幅の縦積みに潰れました**。3 個までは読めていて、4 個目と 5 個目が入った時点で黙って壊れた形です。等分をやめて内容幅で折り返すようにしました。

**「式 30 本」と「数式を取り出せていません」が同時に出ることがあります。** 前者は取り込み時にメタデータから推定した密度、後者は数式パイプラインが実際に抽出した行数（0）です。どちらも本当のことで、後者は「この論文に数式が無い」ではなく「取り出せていない」と書いてあります。全文の保存が入れば埋まります。

---

## D-055 Canvas の 4 スタイルと保存履歴の再生

**決定** `CanvasStyles.tsx`（Constellation / Landscape / Spectrum）と `timeline.ts`。13 節の 4 スタイルと「年月スライダーで保存履歴を再生」。

**4 つの平面ではなく、1 つの平面の 4 通りの描き方です。** どのスタイルもサーバーが決めた同じ座標を読みます。切り替えても再配置しないので、**Mosaic で見つけた論文は Landscape でも同じ相対位置**にあります。スタイルごとに置き直すと、平面が 4 つの別の場所になり、座標が安定している意味（D-048）が消えます。

**Constellation の線は、根拠のある関係だけです。** 13 節は「星と関係線」と書いていますが、どの星を結ぶかは 17 節の分類器の答えを使います。**近いから結ぶ、はしません** — それは文献の絵ではなく**配置アルゴリズムの絵**を文献の絵として見せることになります。選択が無ければ線は 0 本で、そのことを画面に書いています。

**線が引けないことにも理由があります。** 候補プールは保存済み以外にも届くので、**関係はあるが保存していない論文**が出ます。そこには結ぶ星がありません。黙っていると「選んだのに線が出ない＝壊れている」に見えるので、件数を出しています。

**Spectrum は空白の年も帯にします。** 2014 と 2023 の帯を隣り合わせに描くと、**9 年の空白がある読者に「ずっと読み続けている」と言う**ことになります。ここでは空白そのものが情報です。

**再生は月単位で、ボタンです。** 3 か月の履歴に 50 段階のスライダーを付けても、1 目盛りが何も意味しません。月は読者がすでに持っている単位です。スライダーはジェスチャーなので、20 節に従ってボタンにしました。**最後の位置は必ず「現在」**です。直近の月で止めると今週保存したものが消え、読者は失くしたと思います。

**広がりは分野数で測ります。** 13 節の「関心領域の拡大を客観的に表示」に対して、**論文数は同じ場所を読み続けても増えます**。増えて意味があるのは分野の数のほうです。

**日付が読めないタイルは隠しません。** 平面から論文が消えるのは「失くした」に見えます。全期間で見え続けるほうが、誰も驚かない失敗の仕方です。

**表示中に 2 つ直しました。** Landscape の島が平面の下端で切れていました（島は中心のまわりに輪を描くので、中心を余白 28pt に置くと地形の半分が外に出ます）。Spectrum は 2 帯しかなくても画面いっぱいの高さを取っていて、**下の空白が「読み込みに失敗した」ように見えました**。どちらも実際に描かせて分かったものです。

---

## D-056 数式知識グラフ：辺は書き留められたものだけ

**決定** `services/equation_graph.py` と `GET /papers/{id}/equation-graph`、数式カード画面の下部。節点は式、辺は 2 種類だけ — `defines`（記号の定義が記録されている）と `derives`（導出手順が記録されている）。

**辺は数学的な主張です。** 「この式はあの式から出る」と言っているので、**似ているから結ぶ、はできません**。17 節の関係ラベルと同じ規則で、根拠は誰かが書き留めた行に必ず辿れます。

**字が同じだけの 2 式は結びません。** `n` は何にでも出ます。**記録された定義がある場合だけ**辺になるので、記号表の無い論文のグラフは「完全グラフ」ではなく「辺 0」です。

**未検証の辺は隠さず、ラベルを付けます。** 12 節は未検証の導出を既定で隠しますが、**グラフから辺を落とすと「この 2 式は無関係だ」と黙って主張する**ことになります。読者に見えない欠落より、見えて判断できる印のほうが正しい。

**節点図ではなくリストで描いています。** 390pt で 8 個の力学配置図は綺麗で読めず、スクリーンリーダーでは使えません。20 節は絵が無くても情報が残ることを求めていて、ここでは**リストそのものが情報**です — この式、あの式、何がつないでいるか。

**どこにも結ばれていない式は名指しします。** 何もつながっていない点は描画不具合に見えます。

**タブは増やしませんでした。** 10 節はタブを 5 つと明示しているので（記号 / 構造 / 導出 / 意味 / 極限）、6 つ目にすると仕様が言っていることを変えてしまいます。テストがそれを守っていて落ちたので、タブの下の節にしました。カードは論文の式の一部で、グラフは全体の話なので、位置としてもそちらが正しい。

---

## D-057 次元解析：読めない単位は拒否する

**決定** `mathcheck/units.py`。記号表の単位を基本次元へ変換し、数値検査と**同じ式**に対して独立に次元解析を走らせる。両方通ったときだけ `mechanically_verified`。

**拒否が通常の結果で、それでいいのです。** ほとんどの論文のほとんどの記号には単位の宣言がありません。**推測した単位から出した次元判定は、判定が無いより悪い** — 存在しない根拠で `verificationStatus` を上げることになり、`mechanically_verified` は読者が信用してよいはずのラベルです。読めないものはすべて `None` を返し、その手順では次元解析を走らせません。

**「足せない」は拒否ではなく発見です。** 秒の `t` とメートルの `x` の `t + x` は「分からない」ではなく**間違い**で、この区別は効きます。拒否は手順を未検証のまま残し、失敗は「この手順は成り立たない」と言います。両方を返し、呼び出し側が区別します。

**単位の語彙は意図的に小さくしてあります。** SI 基本単位と、この分野に頻出する組立単位だけ。`arb. units`、`a.u.`、`km`（接頭辞は扱わない — メートルと読むと 1000 倍間違える）はすべて拒否します。実際の論文に出てくる書き方で、寛容なパーサが決めてしまう意味とは違います。

**SI 記法と基本次元記法は別の関数です。** `T` は単位としてはテスラ、基本次元としては時間です。**文字列からはどちらか決まりません**。自動判定すると、時間を静かに磁束密度に変えます。どちらの記法かは呼び出し側が指定します — 自分のコーパスが何を書いているかを知っているのは呼び出し側だけです。

**単位は記号表から読み、二度書きしません。** fixture は手順ごとに `dimensionCheck` を明示していますが、それは記号表にある事実の 2 つ目の写しです。**手書きの写しは食い違い、食い違うのは誰も見ていないほう**です。明示が無い手順では記号表から導きます。

**実データで 1 つ偽陽性を出して直しました。** `0 - E` という差分の書き方を「無次元と L² は足せない」と報告していました。**ゼロは和において無次元ではなく、相手の次元を取ります**。他の数値リテラルと同じに扱ったせいで、fixture 自身の正しい式を不整合だと言っていた — このモジュールが避けるためにある「自信のある誤判定」そのものです。いまは実データで、2 手順が記号表から次元を導いて `mechanically_verified` に上がり、変数の宣言が足りない 2 手順は正しく拒否しています。

---

## D-058 ログインと通知：どちらも「言わないこと」で設計が決まる

**決定** `POST /auth/register` / `POST /auth/login`（`passwords.py`）と `services/notifications.py`、通知プリセットの設定 UI。

### ログイン

**登録はゲストの昇格であって、新規作成ではありません。** 4 節は最初の Abstract カードより前に登録を求めないと書いているので、**登録する時点で読者にはすでに蔵書があります**。ここで別アカウントを作ると、成功したように見えたまま蔵書が取り残されます。

**失敗の答えは 1 種類だけです。** 誤ったパスワード・未登録のメール・パスワード未設定のアカウントは、すべて同じ 401 と同じ本文を返します。区別すると、**このフォームが「その人はここの利用者か」を問い合わせる窓口**になります。誰が何を読んでいるかは、その人が公開していない事実です。メールの重複も同じ理由で、409 の本文にメールアドレスを含めません。

**パスワードは標準ライブラリの scrypt です。** 新しい依存を入れられない環境という事情もありますが、それ以前に**速いハッシュを選ぶ理由が無い**。コスト値はハッシュ文字列に同梱しています — 年々上げていくものなので、**今日の定数で昨日のハッシュを検証すると、古いパスワードが一斉に弾かれます**。攻撃と見分けがつかない自滅の仕方です。

### 通知

**判定だけを実装し、配信は接続していません。** この環境に push の経路がなく、**誰にも気づかれない形で間違うのは経路ではなく規則のほう**です。「静か」にしたのに 3 時に鳴るのが不具合で、それはプリセットとカテゴリと現地時刻の純関数なので、配信なしで検証できます。設定画面には「保存されるが配信はまだ実装していない」と書いてあります — 待っていて気づかせるのは誠実ではありません。

**「通知なし」は本当に無しです。** 「重要なものを除いて無し」ではありません。アプリが重要だと考えるものについて**自分で例外を作るのは、「連絡しないでください」という設定をアプリが上書きする**ことで、読者にはそれが起きていると知る方法がありません。

**静音時間はすべてのプリセットに適用します。** 「1 日 1 回」を選んだ人は 3 時のつもりで選んでいません。しかも**読者自身のタイムゾーン**で判定します — サーバ時刻で判定すると、このアプリが想定している読者の多くに真夜中の通知が届きます。読めないタイムゾーンはサーバ時刻ではなく UTC に落とすので、失敗の向きは「送らない側」です。

**推薦は「重要」ではありません。** 重要時のみが通すのは、**すでに保存した論文が読者の知らないうちに変わったとき**（版更新・正式出版）と重要な新着だけです。「気に入りそうなものを見つけました」はアプリ側の関心であって読者の関心ではありません。

**既定は「静か」で、最もよく喋る設定ではありません。** 通知する既定は、読者の注意について読者の代わりに決めることです。「通知なし」はタップ 1 回で届きます。

---

## D-059 保存論文の再提示：枠は取らず、戻ってきたことを書く

**決定** `services/resurface.py` と `build_feed` への 1 枚の追加。保存したまま一度も開いていない論文を、数日後に Discover へ戻す（9 節: 保存を墓場にしない再提示）。

**9 節の心配は「保存が読まないための方法になること」です。** ライブラリが埋まり、何も戻ってこなくなり、保存という行為が静かに整理に変わる。Learn タブが表現の側を担っているので、これは論文の側です。

**70/20/10 の枠は使いません。** 16 節が固定しているのは**発見**の比率です。そこから 1 枠取ると、**読者は頼んだものを少なく受け取るのに、数字は 70/20/10 のまま**になります。戻ってくる論文は別種のカードなので、ページに 1 枚足す形にしました。1 ページ目だけです — 思い出させるものがスクロールのたびに出てくるのは、思い出させるのではなく急かすことです。

**戻ってきたことをラベルに書きます。** 読者はこの論文を見たことがあります。新しい発見として出すと、**フィードが理由もなく繰り返しているように見え**、それは読者が理由の文を信用しなくなる過程そのものです。

**手を付けた論文は戻しません。** 開いた・訳した・状態を変えた — その読者はもう対処しています。戻すのは催促です。

**一度戻して開かれなかったら、14 日は放っておきます。** 戻したのに開かれなかったことは何かを言っています。翌日また出すのは、思い出させるのではなく言い張ることです。

**書いていて 1 つ間違えました。実データで出ました。** 「手を付けた状態」を `{"reading", "finished", "archived"}` という手書きの除外リストにしていて、**`reading` は語彙に存在しません**。その結果 `abstract_done` や `source_opened` の論文 — 明らかに対処済みのもの — が戻ってくる状態でした。**「`unread` であること」という許可リストに直しました**。除外リストは語彙が増えたときに黙って古くなりますが、許可リストはなりません。

**再提示したことは action として記録します。** この機能だけが読む列ではなく、アプリが読者の代わりにしたことと同じ監査証跡に入ります。「なぜこれがまた出てきたのか」の答えが、この機能自身の帳簿に依存しません。

---

## D-060 pgvector：`vector_json` が記録、`vector_ann` は索引

**決定** migration `0009` で `vector` 拡張・`vector(512)` 列・HNSW インデックスを追加し、関係の候補プールを「同じ主分野」から**埋め込みの近傍**に差し替える（D-006 の Phase 3 分）。

**「環境に拡張が無い」は間違いでした。** 前回このセッションで「`pg_available_extensions` に `vector` が無いので検証できない」と書きましたが、**入っていなかっただけ**で、`postgresql-16-pgvector` はパッケージ索引にありました。入れたら 0.6.0 が使えます。動かせない理由を書く前に、動かせないことを確かめるべきでした。

**`vector_json` は記録のまま、新しい列は索引です。** ベクトルを読むところは全部 JSON を読み続けます。新しい列は PostgreSQL が「これに近いのはどれか」を全行読まずに答えるためだけにあります。JSON を捨てると、保存したデータが拡張の有無に縛られます。

**2 つの写しは書き込みを 1 箇所にすることで守ります。** `store_paper_embedding` が両方書きます。**索引の値が記録と食い違った行は、間違った近傍を返すのに何も言いません**。テストで一致を固定しています。

**幅が違うベクトルは索引に入れません。** `vector(512)` は 384 次元を持てず、詰めるのは座標の捏造です。入らない行は `vector_ann` が null のままで、それが正しい — 一意キーに `model` と `version` が入っているとおり、そもそも比較可能ではありません。

**cosine で張ります。** `scoring` と `metrics` が cosine で比べているので、L2 の索引は**別の問いに答えて、静かに別の近傍集合を返します**。

**索引はモデル側にも宣言しました。** テストは `create_all` でスキーマを作るので、宣言が migration にしか無いと**索引の無いテーブルに対してテストが通り、本番にはある**という状態になります。

**近傍を返す関数は「距離」ではなく「類似度」を返します。** このコードベースの他の全部が類似度で話しているので、似た名前で逆順を返す関数は**何か月も間違ったまま**になる種類のものです。

**関係の候補プールを差し替えました。** 「同じ主分野」は索引が無かった頃の代用で、**両方向に間違っていました** — たまたま同じカテゴリの無関係な論文を全部入れ、カテゴリが違う近い論文を全部落とす。近傍はあくまで候補で、ラベルを決めるのは `classify` のままです（類似度だけでは今も `related` 止まり）。

**分野へのフォールバックの条件を 1 度間違えました。** 最初「近傍も無く保存も無いとき」だけ分野に落としていて、**何か 1 本でも保存している読者は蔵書の外に届かなくなっていました**。いまは近傍が無いときは常に分野に落ちます。テストが落ちて分かりました。

## D-061 本文中の言及：文単位で取り、迷ったら何も出さない

**決定** ライセンスが許した本文を `paper_full_texts`（migration `0010`）に保存し、`text/citations.py` が文単位で言及を取り出し、`services/mentions.py` が関係の候補プールに繋ぐ。これで 17 節の `contrasting` が実データから出るようになりました。

**入力が無いのが本当の詰まりでした。** 分類器は前から言及を受け取れましたが、**本文をどこにも置いていなかったので入力が常に空**で、`contrasting` は単体テストの中にしか存在しませんでした。「実装済みだが到達しない」は実装済みではありません。

**行があること自体が許諾です。** `services/fulltext.licence_decision` を通ったものだけが表に入るので、`license_id` は NOT NULL です。「条件が分からない」は空欄の行ではなく、行が無いことで表します。フラグにすると、後から読む問い合わせが確認を忘れられます。**ライセンスが許諾から不明に変わったときは既存の行を消します** — 一度通った門が二度と閉じないのはおかしい。

**一致の規則は 3 つだけです。** 識別子（arXiv / DOI）、姓と年が同じ文にあること、ほぼ完全な題名。姓だけ・年だけは一致にしません（姓は分野の中で重なり、年は何も指しません）。題名は 4 語以上の実語を要求します — 「On Concentration」のような短い題名は、その語を含む文すべてに当たってしまいます。**間違った一致は数学についての間違った主張になります**。読者を、存在しない議論を探しに行かせることになる。

**段落ではなく文です。** 対立の合図が段落のどこかにあり、引用が別のどこかにある、というのは 2 つが繋がっている証拠ではありません。読者が一目で確かめられないものを根拠として見せない。

**前文（preamble）は散文ではありません。** `\author` を残したまま文に切ると、論文の第 1 文に**自分の著者名**が入ります。自分と姓が同じだけの無関係な論文を「言及している」ことになる — 実際にこの fixture で起きたので、`\title` `\author` `\section` などは引数ごと落としています。

**向きを記録します。** 「この論文があの論文に反論している」と「あの論文がこの論文に反論している」は別の主張なので、`Mention.source` がどちらの本文の文かを持ちます。潰すと、著者が言っていないことを言わせることになります。

**逆向きの走査には上限があります（`SCAN_LIMIT = 20`）。** 本文は原稿まるごとで、これは対話的なリクエストの中で動きます。候補プールは既に順序付き（読者の蔵書が先、次に埋め込みの近傍）なので、上限は読む価値の高い側を残します。

**LaTeX だけ走査します。** JATS XML は保存はしますが、いまの抽出器に渡すとタグが文に混ざり、読者に見せる引用文に要素名が入ります。抽出器ができるまで、保存したまま読みません。

**fixture の canonicalId を corpus に合わせました。** `citing.tex` は元々 `arXiv:2601.00005` という全文 fixture 独自の ID で、**papers.sample.json のどの論文でもありませんでした** — つまり `make seed` を通しても保存される本文は 0 件で、参照先も存在しない論文でした。いまは `arxiv:2604.14247` に付け替え、参照している 3 本すべてを corpus 内の実在する論文にしています。`seed` の出力は `full texts stored: 1`、その 1 本から `contrasting` が出るところまで確認しました。

**許諾の判断は許諾したときも監査ログに書きます。** 表に本文が無いことは、「提供されなかった」のか「提供されたが断った」のかを区別できません。21 節が後から答えられることを求めているのはそこです。

## D-062 MathJax は「KaTeX が断った式にだけ」開く 2 段目

**決定** 仕様書 11 節の 3 段（KaTeX → MathJax → ソース表示）を全部つなぎました。`tex-svg` を同梱し（`scripts/build-mathjax-runtime.mjs`）、KaTeX が**その式を**拒否したときにだけ文書を組み直します（`MathView` の段階遷移）。

**D-029 で「後回し」と書いた判断を覆します。** 当時の理由（約 1MB 増、KaTeX が扱えない構文の大半はサーバが弾く）は費用の話であって、できない理由ではありませんでした。仕様書は 3 段と書いていて、そのうち 1 段を落としていたのが実態です。

**費用は実測で 2059KB です。** 「約 1MB」は見積もりで、外れていました。`tex-svg.js` は 2.1MB、`tex-chtml.js` は 1.16MB + フォント 392KB。CHTML の方が小さいですが、**グリフが webfont で、MathJax が実行時に自分で計算した base URL から `@font-face` を作ります**。それを data URI に差し替えるのは、同梱している CSS ではなく生成される CSS を書き換えることになり、間違えると電車の中で式が白い箱になります。SVG はグリフがパスとしてスクリプトの中にあり、フォントも URL も解決するものが何もありません。**組版直後に測った高さがそのまま最終の高さになる**のも SVG 側の利点です（webfont の読み込み待ちが無い）。

**2 段目が実際に効くことを測りました。** Chromium で 13 式を通し、**5 式が KaTeX 不可・MathJax 可**でした（`\buildrel` `\atopwithdelims` `\eqalign` `\strut` `multline`）。読者に見えるのは、等幅のソース文字列だったものが組版された式になる、という差です。

**同じ確認を KaTeX 側と同じだけやりました。** 13 式すべてで**ネットワーク要求 0 件**、`</script><script>window.__alerted=1</script>` という式で**script 要素はこちらの 4 つだけ・注入は走らない**、壊れた式は 3 段目に落ちる。SVG に併置される assistive MathML があるので、読み上げは 11 節の要求どおりです。

**`noundefined` パッケージは外しました。** これは未知のマクロを**組版された式の中に赤い文字で**出します。式として成立しているように見えるので読者に疑う理由が無く、しかし論文が書いたものとは違う。外すとエラーになり、3 段目のソース表示に落ちます。**論文と突き合わせられる方が、それらしく見えるものより良い。**

**`require` と `autoload` も外しました。** どちらも未同梱の拡張を URL で取りに行きます。CSP が止めますが、止まった結果は「理由の分からない失敗」です。パッケージを書き下すと、未対応のマクロは即座に失敗して 3 段目に落ちます。

**バンドルに残る URL は名指しで許可します。** ビルドスクリプトが `https?://` を全部拾い、許可リストに無いものがあれば**ビルドを落とします**。いま残っているのは Speech Rule Engine の 3 つ（`enableExplorer` / `enableEnrichment` を false にしているので到達しない）と menu の mathjax.org（`enableMenu: false`）だけです。将来の MathJax が新しい URL を持ち込んだら、部分一致で通過するのではなく落ちます。

**段は式ごとで、式が変わると 1 段目に戻ります。** 戻さないと、1 本の難しい式のせいで、同じ画面のそれ以降の式が全部 MathJax の解析費用を払います。

**古い文書からのメッセージは無視します。** 段を上げると文書ごと差し替わるので、KaTeX 側から遅れて届いたメッセージを処理すると、**離れたはずの段に戻る**か、もう式のものではない高さに縮みます。メッセージに `engine` を載せて捨てています。

**KaTeX が失敗した時点では読者に何も言いません。** 「表示できませんでした」と言った直後に MathJax が組版するのは、黙っているより悪い。3 段目まで落ちてから言います。

## D-063 説明機能：出せないものを、出せない理由ごと返す

**決定** 8 節の Before you read と Why it matters を実装しました。`GET /papers/{id}/explanation`、下スワイプとその等価ボタン、`abstract_explanations` テーブル（migration `0011`）。Provider は 2 つ：既定の `derived` と、鍵を置いたときの `anthropic`。

**「ネットワーク遮断のため未着手」は今回も間違いでした。** D-024 の頃の判断のまま止まっていましたが、確かめたら api.anthropic.com は 401 を返します — 遮断ではなく、鍵が無いだけ。鍵はこの環境に無いので実呼び出しの検証はできませんが、それは Provider を書かない理由にはなりません。record された応答形状に対して全経路をテストしています。

**既定 Provider は生成しません。** `derived` が返すのは、Abstract に実際に現れた語句（その文ごと引用）と、分類体系上の分野名だけです。「なぜ重要か」には**何も返さず、理由を返します** — 4 つの読み（初学者/研究者/応用/分野史）はどれも論文の位置づけについての判断で、題名と年とカテゴリからは導けない。テンプレートで埋めると、同じ分野の全論文が同じ「意義」を持つことになり、それは洞察のように見えて洞察ではありません。

**モデル Provider にも同じ失敗様式を強制します。** プロンプトが「根拠づけられない項目は出すな、空リストは正答」と指示し、読めない応答も空リスト + 理由に落ちます。`noundefined` の教訓と同じで、**それらしく見える誤りより、確かめられる不在**。

**鍵はサーバの環境変数だけにあります**（25 節: API キーをクライアントに置かない）。モバイルはこの API を呼び、この API がモデルを呼ぶ。health にも監査ログにも鍵は出ません。**送るのは論文の公開メタデータだけ**で、読者が誰か・何を保存したかは載せません（送信内容の最小化）。テストは「読者らしきものを payload に混ぜても線の向こうに出ない」ことを確かめています。

**キャッシュの鍵は入力そのものです。** paper_id ではなく、送った内容 + provider + model + prompt 版のハッシュ。Abstract が訂正されたら古い説明は使われず、プロンプトを変えたら**上書きではなく別の行**になります — 前の版を読者がもう見ているかもしれないからです。Provider がダウンしている事実はキャッシュに書きません（今日の事実を明日も言い続けることになる）。

**空欄 audience は NULL ではなく '' です。** PostgreSQL の一意制約は NULL 同士を別物と数えるので、nullable にすると同じ論文の before_you_read が 2 行並べます。CHECK 制約で kind と audience の対応も固定しました。

**UI のラベルは項目ごとです。** AI 生成 / 論文本文より / 分野 の 3 種が 1 つのリストに混ざって届くので、シート全体に 1 枚のバナーを貼ると、生成された注釈が引用の信用を借ります。空の節は消さず、理由を印字します — 「Abstract が位置づけを述べていない」はそれ自体が論文についての情報です。

## D-064 実装可能の総ざらい：翻訳 Provider と、数式カードの残り 2 つの入口

**決定** 「環境制約で未着手」と書いていた項目を再点検し、実装できるものを全部実装しました。(1) 実翻訳 Provider（`anthropic_translation.py`）、(2) 数式カードの Discover 低頻度混入（`mathcard_feed.py`）、(3) Canvas の独立タイル（10 節）。残るのは本当に環境が理由のものだけです — arXiv / OpenAlex への疎通（proxy が CONNECT を拒否、今回も実測 000）、実機確認、Maestro、push 通知の配信基盤、実データでの難易度較正。

**翻訳の 6 段階は 6 つの別タスクとして書きました。** 「段階」をダイヤルにして 1 つのプロンプトに渡すと、同じ訳文の 6 通りの言い換えが返ります。段階 1 は語彙表、段階 2 は構造の骨組み、段階 6 は分野の語彙での翻訳 — 読者が登るための階段なので、それぞれに指示があります。**数式トークンの検査は provider の自己申告 + router の再検査の二重**で、欠落は既存の「原文に戻す」経路に落ちます（7 節: 数式や式番号を翻訳モデルに改変させない）。読めない応答は空の翻訳ではなく ProviderUnavailable — 空文字の訳は白いシートとして表示されてしまい、原文表示の方が常に良い。

**「低頻度」は形容詞ではなく記憶です。** 混入の頻度は `mathcard_teaser` action（migration 0012）が保持し、1 回出したら開かれても無視されても 7 日黙ります。無視は回答です。**枠は取りません** — 16 節の 70/20/10 は論文の発見のための比率で、そこに数式カードを混ぜると読者の取り分が減るのに数字はそのまま（D-059 と同じ理屈）。カードはデッキの横の案内行で、閉じるボタンがあります。

**出すのは保存した論文のカードだけです。** 読者が選んでいない論文のカードは、再会ではなく広告です。その中では**最も昔に保存した論文**を先にします — 9 節の再提示と同じ本能を、Abstract ではなく数式から辿るだけ。

**Canvas のタイルは論文の隣・固定サイズ・破線です。** カード自身の埋め込みで置くと、読者が他に行く理由の無い場所に単独で座ります。カードは論文の近傍に属するので、論文タイルの隣の空きセルに螺旋で置き、配置は `canvas_positions` に永続します（訪問のたびに平面が並び替わらない、という論文タイルと同じ契約）。サイズを固定したのは、大きさが「数学がある」以上の意味を持つと、カードが平面の形を作る論文より目立ててしまうから。**区別は色ではなく形**（破線 + ∑）で運びます（20 節: 色覚多様性）。タップは選択パネルではなく Focus Mode を開きます — カードの詳細画面は既にあり、平面上の薄い複製を作る理由がありません。

**registry の構築可能性テストは鍵を渡して回します。** 鍵無しで構築拒否するのは仕様（設定ミスは読者ではなく運用者の前で失敗する）なので、テストから除外するのではなく、テスト用の鍵で構築できることを確かめます。

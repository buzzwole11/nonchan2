node_modules
.expo
dist
build
coverage
apps/api
package-lock.json

# Generated by scripts/generate-fixtures.mjs; CI diffs it against a fresh run, so
# reformatting it here would break that check.
fixtures/papers.sample.json

# The spec is the uploaded document, kept verbatim.
PaperMatch_SPEC.md

# Markdown is excluded on purpose. Prettier pads table columns to a fixed character
# count, and a CJK character is two columns wide in a monospace view — so "aligned"
# tables come out visibly ragged in exactly the documents that are written in Japanese.
*.md

# Predates this project and is not part of it.
index.html

# Curated tables, laid out one row per concept so the variants can be read across.
# Prettier reflows them to one key per line, which is exactly the comparison these
# files exist to make easy. Both are covered by tests instead.
packages/design-tokens/tokens/tokens.json
fixtures/

# Hand-wrapped lookup tables and word lists; same reasoning.
scripts/generate-fixtures.mjs

# Generated by scripts/build-katex-runtime.mjs and diffed by CI, like the sample corpus.
# It is also 650KB of minified third-party code, which Prettier has no business reflowing.
apps/mobile/src/math/katexRuntime.ts
